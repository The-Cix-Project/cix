# Architecture Decision Records

See [0000-adr-process.md](0000-adr-process.md) for what an ADR is for and how it's kept here (short-lived reasoning that lives forever, append-only, distinct from `docs/roadmap/ROADMAP.md`'s what-shipped narrative).

| ADR | Title | Status |
|---|---|---|
| [0000](0000-adr-process.md) | Recording architecture decisions | Accepted |
| [0001](0001-tcc-exclusive-toolchain.md) | TCC exclusively, dynamically linked against system glibc | Accepted |
| [0002](0002-raw-syscalls-for-missing-glibc-wrappers.md) | Raw syscalls where glibc has no wrapper | Accepted |
| [0003](0003-clone3-atomic-cgroup-placement.md) | clone3 + CLONE_INTO_CGROUP + CLONE_PIDFD over legacy clone() | Accepted |
| [0004](0004-overlayfs-dedicated-lowerdir.md) | OverlayFS lowerdir is a dedicated, purpose-built tree | Accepted |
| [0005](0005-api-first-mandate.md) | API-First Mandate: the REST daemon is the only path to the runtime | Accepted |
| [0006](0006-openapi-spec-format.md) | OpenAPI 3.0 YAML as the API spec format | Accepted |
| [0007](0007-hand-rolled-daemon-no-external-libs.md) | Hand-rolled HTTP/JSON/reactor in the daemon, no external libraries | Accepted |
| [0008](0008-tcc-pragma-pack-for-kernel-abi-structs.md) | Use #pragma pack, not __attribute__((packed)), for kernel-ABI structs under TCC | Accepted |
| [0009](0009-cloexec-daemon-fds-before-clone3.md) | CLOEXEC every daemon-owned fd before any clone3() call | Accepted |
| [0010](0010-vanilla-web-dashboard-no-build-step.md) | Vanilla HTML/CSS/JS dashboard, no framework, no build step, served by kanxeod | Accepted |
| [0011](0011-rtnetlink-control-plane-over-kernel-bridge.md) | rtnetlink control plane over the kernel's own bridge/veth, not a userspace switch | Accepted |
| [0012](0012-networks-json-persistence.md) | Atomically-rewritten flat file for network persistence, not a database | Accepted |
| [0013](0013-proc-pid-root-for-live-container-file-writes.md) | /proc/<pid>/root/ for writing into a running container's filesystem, not raw upperdir or setns() | Accepted |
| [0014](0014-squashfs-ab-root-with-native-boot-counting.md) | Read-only squashfs A/B root with systemd-boot's native boot counting, not a writable root or hand-rolled rollback | Accepted |
| [0015](0015-shim-mok-secure-boot-signing.md) | shim + MOK enrollment for Secure Boot on the installed system, installer media stays unsigned | Accepted |
| [0016](0016-reboot-syscall-for-kanxeod-shutdown.md) | kanxeod calls reboot(2) as PID 1, exposed via REST, SIGTERM/SIGINT default to poweroff | Accepted |
| [0017](0017-ebpf-cgroup-device-filter-for-hardware-passthrough.md) | BPF_CGROUP_DEVICE for device passthrough enforcement, not a blanket eBPF ban | Accepted |
| [0018](0018-containers-partition-for-base-dir-persistence.md) | The real kanxeo-containers partition backs BASE_DIR, not a fresh tmpfs every boot | Accepted |
| [0019](0019-runtime-libs-seeded-at-install-time.md) | The shared "base" image's C runtime is seeded at install time, not by pkg or at boot | Accepted |
| [0020](0020-per-image-pkg-install-compound-key.md) | pkg install targets an explicit image, tracked by a (name, image) compound key | Accepted |
| [0021](0021-explicit-network-ip-override.md) | A container's network attachment IP can be explicitly chosen, not only auto-allocated | Accepted |
| [0022](0022-real-nic-passthrough-netns-move.md) | Real network interface passthrough via direct netns move, with fd-anchored teardown | Accepted |
| [0023](0023-per-image-runtime-seeding.md) | The C runtime is seeded into every image, not only "base" | Accepted |
| [0024](0024-image-lifecycle-endpoints.md) | Image lifecycle as a filesystem-backed REST resource, no separate persistence | Accepted |
| [0025](0025-persisted-auto-restarting-containers.md) | Persisted, auto-restarting containers (`restart: "always"`, `depends_on`) | Accepted |
| [0026](0026-tcp-readiness-checks-for-depends-on.md) | TCP readiness checks for `depends_on` | Accepted |
| [0027](0027-restart-policy-expansion-and-backoff.md) | Restart policy expansion (`on-failure`, `unless-stopped`) and crash-restart backoff | Accepted |
| [0028](0028-gpu-passthrough-grouped-device-grants.md) | GPU passthrough: grouped multi-node device grants | Accepted |
| [0029](0029-gpu-kernel-driver-firmware-and-kfd.md) | GPU kernel driver, firmware staging, and KFD discovery | Accepted |
| [0030](0030-per-container-config-files-and-sysctls.md) | Per-container config files and generalized sysctls | Accepted |
| [0031](0031-host-and-package-update-mechanism.md) | Host OS update mechanism (write to inactive A/B slot), and automatic package updates | Accepted |
| [0032](0032-per-slot-kernel-updates.md) | Per-slot kernel files, extending POST /system/update to cover bzImage | Accepted |
| [0033](0033-platform-state-backup-restore.md) | Platform state backup/restore -- configuration only, not workload data, never PKI | Accepted |
| [0034](0034-console-login-via-supervised-kanxeoctl.md) | Console login via a kanxeod-supervised kanxeoctl, not a general shell | Accepted |
| [0035](0035-portable-toolchain-artifact-for-pkg-bootstrap.md) | Portable toolchain artifact for pkg bootstrap, not just a live-host copy | Accepted |
| [0036](0036-multi-source-package-recipes.md) | Multi-source package recipes | Accepted |
| [0037](0037-network-gateway-optional.md) | A network's host-owned gateway becomes optional, default flips to gateway-less | Accepted |
| [0038](0038-vlan-and-physical-nic-bridge-attachment.md) | VLAN via 802.1q sub-interfaces, physical NIC attachment via the existing rtnl_link_set_master | Accepted |
| [0039](0039-package-recipes-seeded-at-install-time.md) | Package recipes seeded at install time, same mechanism as ADR-0019 | Superseded by [0040](0040-package-recipes-managed-via-rest-api.md) |
| [0040](0040-package-recipes-managed-via-rest-api.md) | Package recipes are managed via a real REST API, not baked into the installer ISO | Accepted |
| [0041](0041-container-image-baseline-fhs-layout.md) | Container images get a real, fixed baseline FHS layout at seed time | Accepted |
| [0042](0042-installer-dual-console-pty-relay.md) | The installer relays interactive I/O through a PTY across both consoles at once | Accepted |
| [0043](0043-container-console-exec-websocket.md) | An interactive shell into a running container, over a hand-rolled WebSocket | Accepted |
| [0044](0044-daemon-data-dir-test-isolation.md) | `kanxeod --data-dir=` for test/production state isolation | Accepted |
| [0045](0045-container-start-pause-lifecycle.md) | Container start/pause: cgroup-freeze lifecycle completeness | Accepted |
| [0046](0046-site-scoped-dns-pki-naming.md) | Site-scoped DNS/PKI naming: a real, configurable convenience, not enforcement | Accepted |
| [0047](0047-two-tier-pki-intermediate-ca.md) | Two-tier PKI: an intermediate CA for day-to-day signing | Accepted |
| [0048](0048-persistent-device-name-mappings.md) | Persistent device name mappings: exact vs. vendor/model resolution | Accepted |
| [0049](0049-pki-ca-reset-regeneration.md) | PKI CA reset/regeneration: reissue-in-place, not destroy | Accepted |
| [0050](0050-auto-issued-host-pki-cert.md) | Auto-issued host PKI cert: a fixed record name, not the FQDN itself | Accepted |
| [0051](0051-ca-trust-staged-into-images.md) | CA trust chain staged into every image at creation time | Accepted |
| [0052](0052-server-side-default-name-qualification.md) | Server-side default DNS/PKI name qualification (revises ADR-0046) | Accepted |
| [0053](0053-auto-maintained-instance-dns-record.md) | Auto-maintained instance DNS record | Accepted |
| [0054](0054-host-side-container-stats.md) | Host-side per-container stats: raw point-in-time snapshot, no server history | Accepted |
| [0055](0055-container-file-read-endpoint.md) | Container file-read REST endpoint: raw bytes, running-vs-exited path resolution | Accepted |
| [0056](0056-hostbuild-artifact-mechanism.md) | Hostbuild artifact mechanism: a second pkg-install mode that harvests a standalone artifact instead of merging into an image | Accepted |
| [0057](0057-self-hosted-toolchain-and-control-plane-rebuild.md) | Self-hosted toolchain and control-plane rebuild: tcc.recipe, kanxeo.recipe, server-side mkbootroot | Accepted |
| [0058](0058-host-management-network-unification.md) | The host's own management address becomes a real, API-managed network | Accepted |
| [0059](0059-openssl-https-listener.md) | OpenSSL-backed HTTPS listener, reusing the PKI host cert, via a small TLS side-table | Accepted |
| [0060](0060-cpu-bandwidth-and-affinity-api-exposure.md) | Expose cpu.max and cpuset.cpus through the API/CLI, no reinterpretation layer | Accepted |
| [0061](0061-kernel-module-loading.md) | Kernel module loading: root-critical drivers stay built-in, only network/USB become modules | Accepted |
| [0062](0062-ext4-project-disk-quotas.md) | Disk quotas: real ext4 project-quota enforcement, permanent project-id assignment | Accepted |
| [0063](0063-iso-self-build-toolchain.md) | ISO self-build: a real, from-source GRUB2/xorriso/mtools/sbsigntools toolchain | Accepted |
| [0064](0064-rest-driven-iso-assembly.md) | REST-driven ISO assembly: closing the API-First Mandate gap ADR-0063 left open | Accepted |
| [0065](0065-pkg-bootstrap-url-fetch.md) | pkg bootstrap URL-fetch: closing a real gap in ADR-0031's own "operator scp's it" assumption | Accepted |
| [0066](0066-kernel-routing-table-diagnostic.md) | A real kernel routing-table diagnostic: closing the last blind spot in "the daemon is the only way to inspect a running box" | Accepted |
| [0067](0067-network-address-field-rename.md) | Rename a network's `gateway`/`has_gateway` field to `address`/`has_address` | Accepted |
| [0068](0068-dedicated-daemon-bind-ip.md) | A dedicated daemon bind IP, decoupled from the management network's own address | Accepted |
| [0069](0069-host-swap-file.md) | A single, on-demand host swap file: direct swap-header write, no `mkswap` dependency | Accepted |
| [0070](0070-consolidated-log-store.md) | A consolidated, API-accessible, size-capped log store: kernel dmesg + kanxeod diagnostics + per-request audit trail | Accepted |
| [0071](0071-disk-format-mount.md) | Disk format + mount as a separate, explicitly-confirmed action; a non-execve()ing job child for its two sequential steps | Accepted |
| [0072](0072-log-message-cap-and-min-level.md) | Log message cap raised 512->4096, build-output capture switched head->tail, settable write-time minimum severity | Accepted |
| [0073](0073-host-stats-endpoint.md) | Host-wide stats endpoint (GET /v1/system/stats): load/CPU/memory/disk/network, mirroring per-container stats | Accepted |
| [0074](0074-pressure-stall-information.md) | Pressure-stall information (PSI) in host and per-container stats: cpu/io/memory.pressure | Accepted |
| [0075](0075-icmp-reachability-endpoint.md) | Reachability endpoint (GET/POST /v1/system/ping): real, hand-rolled ICMP echo | Accepted |
| [0076](0076-host-dns-resolver-config.md) | Host DNS resolver config (GET/PUT /v1/system/resolv) + a real /etc/resolv.conf bind-mount fix | Accepted |
| [0077](0077-health-boot-identity-split.md) | Split GET /health into liveness + GET /system/boot identity | Accepted |
| [0078](0078-from-source-host-tools-bootstrap.md) | From-source host tools bootstrap (mkbootroot.c's shelled-out binaries) | Accepted |
| [0079](0079-cgroup-controller-delegation.md) | cgroup v2 controller delegation at daemon startup (real-PID-1/no-systemd container-create 500 fix) | Accepted |
| [0080](0080-container-diagnostics-visibility.md) | Container creation/exit diagnostics visibility -- real errno text, not just numeric codes | Accepted |
| [0081](0081-cgroup-controller-delegation-atomic-write-fix.md) | cgroup controller delegation: fix the atomic-write regression ADR-0079 introduced | Accepted |
| [0082](0082-kernel-smp-missing.md) | CONFIG_SMP was never enabled -- uniprocessor kernel, and the real root cause of the cpuset gap | Accepted |
| [0083](0083-mkbootroot-ld-linux-second-copy.md) | mkbootroot's own control-plane root was missing the second ld-linux copy | Accepted |
| [0084](0084-mkbootroot-mksquashfs-unreachable-on-real-host.md) | mkbootroot's own mksquashfs exec target was never reachable on a real installed host | Accepted |
| [0085](0085-mkbootroot-source-paths-not-merged-usr.md) | The real, final root cause: mkbootroot's own source reads assumed a merged-usr host | Accepted |
| [0086](0086-pkgbuild-stop-bypasses-pkg-completion.md) | Stopping __pkgbuild via POST .../stop left pkg.c's job lock stuck forever | Accepted |
| [0087](0087-pkg-build-output-pipe-deadlock.md) | Build-output capture pipes were drained once, after exit, deadlocking any sufficiently verbose build | Accepted |
| [0088](0088-kernel-veth-missing.md) | CONFIG_VETH was never enabled -- every container network attachment on a real box has always failed | Accepted |
| [0089](0089-root-netns-ip-forward-missing.md) | root-netns net.ipv4.ip_forward was never enabled -- no container could ever originate a connection past the host | Accepted |
| [0090](0090-container-create-log-use-after-free.md) | container-create failure logging used a dangling JSON-tree pointer | Accepted |
| [0091](0091-dns-server-binding-not-persisted.md) | DNS server bindings were never persisted, silently lost on every restart | Accepted |
| [0092](0092-dns-register-missing-site-qualification.md) | auto-registered container DNS records were never site-qualified, unlike manual ones | Accepted |
| [0093](0093-extract-tarball-assumed-wrapping-directory.md) | extract_tarball() assumed every source tarball has a wrapping directory | Accepted |
| [0094](0094-hostbuild-upgrade-flag.md) | pkg hostbuild had no upgrade/force path, a bare permanent 409 | Accepted |
| [0095](0095-update-one-sided-footgun.md) | POST /system/update's one-sided image_path/kernel_path footgun | Accepted |
| [0096](0096-pkg-fetch-diagnostic-capture.md) | pkg fetch failures reported only a bare curl exit code, curl's own real error text was discarded | Accepted |
| [0097](0097-mkbootroot-missing-ca-bundle.md) | mkbootroot never staged a CA certificate bundle -- every host-side HTTPS pkg_source fetch had no way to verify a TLS cert | Accepted |
| [0098](0098-tarball-listing-truncation-false-mismatch.md) | tarball_has_common_top_dir() false-positived "no common top dir" when its own 64KB capture truncated mid-line | Accepted |
| [0099](0099-disk-mount-ground-truth.md) | GET /disks reports real mount status from /proc/mounts, not diskformat.c's own ephemeral job history | Accepted |
| [0100](0100-container-cmd-visibility.md) | GET /containers echoes cmd, sourced from registry_entry (live) or the persisted body (stopped) | Accepted |
| [0101](0101-pkg-build-log-live-tail.md) | GET /v1/pkg/build/log live-tails an in-flight build's own output, a one-way WS relay distinct from the console's exec/PTY session | Accepted |
| [0102](0102-per-container-disk-selection.md) | Per-container disk selection at creation time (Phase D) -- POST /containers' new "disk" field, gated on the container-storage role | Accepted |
| [0103](0103-btrfs-quota-backend.md) | Real btrfs qgroup-based disk quotas as a second backend alongside ext4 project quotas, closing the --disk-quota= filesystem gap | Accepted |
| [0104](0104-btrfs-disk-format.md) | fs_type on the disk-format REST endpoint -- real btrfs disk formatting alongside ext4 (btrfs-progs.recipe, libblkid.recipe) | Accepted |
| [0105](0105-bootroot-assembly-freshness.md) | Real generation-counter freshness tracking for `pkg hostbuild kanxeo --deploy`, closing the stale-artifact silent-redeploy gap | Accepted |
| [0106](0106-container-delete-disk-cleanup.md) | DELETE /containers actually removes on-disk upper/work/merged state, unmounting the overlay first | Accepted |
| [0107](0107-package-image-versioning.md) | Package + image versioning model: version-keyed recipes, per-image pinned/rolling manifests, per-version immutable image rootfs | Accepted |
| [0108](0108-image-version-content-hash.md) | Image versions are a manifest-content hash (sha256 of resolved package@version list), not a sequential integer; explicit created_at tracking for both image and recipe versions | Accepted |
| [0109](0109-ldap-redesign.md) | LDAP redesign: glauth, config-render write-through (mirroring DNS, not a second SQLite-owned datastore), and owner-tracked automatic service-account provisioning | Accepted |
| [0110](0110-ntp-host-clock-sync.md) | NTP: hand-rolled SNTP client for the host clock (clock_settime() needs host-namespace CAP_SYS_TIME, no container can do it), plus container time-source registration mirroring DNS/LDAP | Accepted |
| [0111](0111-ssh-ldap-account-sync.md) | Jump box SSH auth backed by LDAP: rendered Unix accounts (passwd/group/shadow/authorized_keys), not real NSS/PAM-over-LDAP | Accepted |
| [0112](0112-container-output-capture.md) | Opt-in stdout/stderr capture for ordinary containers, reusing the existing build-container pipe/epoll-drain mechanism -- closes the diagnostic gap named by tasks #731 and #747 | Accepted |
| [0113](0113-glauth-listener-mystery-resolved.md) | glauth's "listener mystery" resolved for real: an out-of-range diagnostic IP colliding with a real LAN device, plus a genuine missing CONFIG_INOTIFY_USER kernel gap -- neither a Kanxeo nor a glauth defect | Accepted |
| [0114](0114-kernel-config-gap-config-seccomp.md) | Fourth allnoconfig-starting kernel config gap: CONFIG_SECCOMP was never enabled, so every SSH login's own preauth sandbox setup failed and reset the connection | Accepted |
| [0115](0115-delete-crashed-container-disk-cleanup.md) | DELETE's task #738 disk cleanup never ran for a crashed-but-defined container, letting a stale upperdir's wrong file permissions crash-loop a restart:"always" container forever under a reused name | Accepted |
| [0116](0116-json-parser-u-escape-support.md) | json_parse() gains narrow \uXXXX support, closing a write/parse asymmetry that silently broke kanxeoctl ps whenever a container's captured_output held a colorized program's ANSI escapes | Accepted |
| [0117](0117-dns-record-qualify-on-read.md) | DNS record GET/PUT/DELETE now qualify a bare name the same way POST already does -- a record created with --name=foo could only ever be looked back up by its full site-qualified FQDN | Accepted |
| [0118](0118-boot-init-devpts-mount.md) | boot_init() mounts /dev/pts: the real root cause of every live console/exec 500 on an installed box -- kanxeo-install.c's own devpts mount was never carried over to kanxeod's own separate real-PID-1 boot path | Accepted |
| [0119](0119-container-run-tmpfs.md) | /run is a fresh tmpfs on every container start, not overlay-persisted storage -- root cause of chronyd's respawn self-collision (stale PID-1 pidfile) that made ntp-1/ntp-2 never survive a reboot | Accepted |
| [0120](0120-pkg-redesign-part1-naming-and-backup-fix.md) | pkg/ redesign Part 1: recipe.sh -> build.sh rename, plus a real backup/restore regression found and fixed along the way (system backup had included zero recipes since ADR-0107's version-keyed layout landed) | Accepted |
| [0121](0121-pkg-redesign-part2-configurable-repo-and-sync.md) | pkg/ redesign Part 2: configurable recipe repo (gitea/github/gitlab) + merge/additive `pkg sync`, real-only-for-gitea fetch verification | Accepted |
| [0122](0122-pkg-redesign-part3-artifact-cache-and-server.md) | pkg/ redesign Part 3: local build-artifact cache with LRU eviction + a plain-HTTP (never git) precompiled-artifact server, checksum-verified against the recipe's own git-tracked hash | Accepted |
| [0123](0123-pkg-redesign-part4-image-recipes-and-artifact.md) | pkg/ redesign Part 4: declarative image recipes (bulk-declare a manifest) + a whole-rootfs artifact fast path for fully-pinned recipes, reusing Part 3's plain-HTTP server under a distinct URL prefix | Accepted |
| [0124](0124-pkg-redesign-part5-rolling-containers-and-restart-jitter.md) | pkg/ redesign Part 5: opt-in per-container `follow_rolling`, reconciled after every pkg-job completion, with a configurable jittered-restart window to spread out simultaneous restarts; found and fixed a real timerfd zero-`it_value`-disarms-not-fires-immediately bug along the way | Accepted |
| [0125](0125-pkg-delete-failed-state-and-jitter-override.md) | Part 5 follow-up: `pkg_delete()` now clears a permanently-`PKG_STATE_FAILED` entry directly (nothing was ever really merged into an image, so no image version is produced) alongside the existing `PKG_STATE_INSTALLED` uninstall path; `follow_rolling` gains a per-container `follow_rolling_jitter_seconds` override of the daemon-wide default | Accepted |
| [0126](0126-transparent-container-log-capture.md) | Logging/web-UI epic Part 1: every container's stdout/stderr is now unconditionally captured into the existing consolidated log store (not opt-in, not a second store), with new `container=`/`regex=` filters; `capture_output` narrows to controlling only the separate per-container `captured_output` tail; also fixes a live TLS-handshake-error console-spam bug found along the way | Accepted |
| [0127](0127-syslog-forward-targets.md) | Logging/web-UI epic Part 2: optional, redundant `syslog-1`/`syslog-2` forward targets -- `POST`/`GET`/`DELETE /v1/syslog/targets` registers a running container (mirrors NTP server registration), every container-sourced log line is also sent as a real RFC 3164 UDP datagram, alongside (never instead of) the consolidated log store, which stays the one source of truth | Accepted |
| [0128](0128-host-owner-sentinel.md) | Logging/web-UI epic Part 5: the daemon's own auto-issued "host" PKI cert and auto-maintained instance DNS record now carry a reserved `"__host"` owner sentinel (rendered as "host (this daemon)" in CLI/web) instead of `null`, distinguishing "owned by the daemon itself" from "no owner for any other reason" | Accepted |
| [0129](0129-merged-log-panel-and-toast.md) | Logging/web-UI epic Part 3: the web dashboard's bottom action-log panel now merges server logs (kernel/kanxeod/audit/container) with its own client-side "web-ui" activity into one filtered stream; the Logs tree page keeps only the size-cap config; the status bar becomes a fixed-position, auto-dismissing toast that also lands in the merged log panel | Accepted |
| [0130](0130-host-stats-graphs-page.md) | Logging/web-UI epic Part 4: a new System > Server > Host Stats page graphs `GET /system/stats` (load/CPU/memory/disk/network), reusing the container Stats tab's own `drawChart()` canvas renderer -- host-side CPU% uses the classic `/proc/stat` idle-delta formula since the source data is raw jiffies, not a cgroup usage counter | Accepted |
| [0131](0131-host-process-list-and-kill.md) | Logging/web-UI epic Part 6 (final): `GET`/`DELETE /v1/system/processes` -- a real `/proc` scan of every host process, each correlated to a container (if any) via a real host ppid-chain walk against the registry's own known container root pids; kill is a real, immediate SIGKILL, refusing only pid 1 and this daemon's own pid | Accepted |
| [0132](0132-log-panel-fixes-and-cli-polish.md) | Real-world fixes after the logging epic shipped: log panel fixed-size CSS bug (`min-height: 0`) + batched DOM rendering + generalizing the audit log's `GET /health` exclusion to every `GET` (fixes both the visual growth and the dashboard slowdown, same root cause); `resolv_set()` switched from a rename-based to an in-place write so `PUT /v1/system/resolv` actually stays live across the bind-mounted `/etc/resolv.conf` instead of silently detaching until the next reboot; dynamic `<instance_name>>` shell prompt; `process ls` restyled to match `ps`; new `container ls` synonym | Accepted |
| [0133](0133-lenient-repo-url-parsing.md) | `parse_repo_url()` (pkg repo sync) now reads only the first two path segments as owner/repo instead of splitting at the last slash -- a real forge browse URL pasted verbatim (e.g. gitea's own `.../owner/repo/src/branch/<ref>/<path>`) now resolves correctly instead of silently mis-parsing into a garbage owner and an opaque 404 | Accepted |
| [0134](0134-tls-handshake-throttling.md) | Per-source-IP throttling for repeated failed HTTPS handshakes: the peer IP is now logged, a source that fails enough within a window is refused outright on both listeners, loopback is never throttled (avoids locking out the daemon's own local admin access), fully API-driven and configurable | Accepted |
| [0135](0135-tls-throttle-log-rate-limit-and-ca-download.md) | ADR-0134 follow-up: the real flood source turned out to be the user's own untrusted-cert browser, not a hostile source -- rate-limited the handshake-failure log line independently of the block threshold, and added a CA cert download button + platform-specific trust instructions | Accepted |
| [0136](0136-https-listener-serves-full-chain.md) | ADR-0135 follow-up: the HTTPS listener was only ever sending its leaf cert during the handshake, never the intermediate CA, so a client trusting only the root (the documented workflow) could never actually validate the chain on a box with an intermediate bootstrapped -- fixed via `SSL_CTX_add_extra_chain_cert()`; also renamed the CA download button's file from `.pem` to `.crt` | Accepted |
| [0137](0137-tls-throttle-block-scoped-to-https-listener.md) | ADR-0134 revisited: a source blocked for repeated failed HTTPS handshakes no longer also loses plain HTTP access -- only a failed TLS handshake can ever cause a block, so enforcing it against the unrelated listener stranded a legitimate fallback path (found live: the user's own untrusted-cert desktop retrying HTTPS lost HTTP access too) | Accepted |
| [0138](0138-web-dashboard-ia-rework.md) | Web dashboard information-architecture rework: System > Server (10 leaves) split into Host/Monitoring/Maintenance sized like its siblings; Software split into Catalog/Build Pipeline; PKI's overloaded "Root CA" leaf split into Root CA + Intermediate CA; NTP's Status+Time merged into Time & Sync; Route creation moved to the shared `+ Create` modal; Container detail's Summary absorbed Stats and the content-free Backup tab was removed, Summary became the default tab; Network detail gained a "Containers on this network" table (a real, previously-unfilled data gap) -- zero API/daemon changes, pure client-side reorganization | Accepted |
| [0139](0139-web-dashboard-theme-and-last-view-memory.md) | Web dashboard: a header button cycles Auto/Light/Dark (new `:root[data-theme]` CSS overrides layered on the existing `prefers-color-scheme` support, no flash on load via an inline bootstrap script), and a bare load with no hash now restores the last-viewed page instead of always starting at Containers -- tree collapse memory was investigated and found already correct, no code change needed there | Accepted |
| [0140](0140-web-dashboard-disk-management.md) | Web dashboard: new System > Host > Disks page closes a real API/UI parity gap -- `GET /disks`/`diskroles`/`disks/{name}/format` had existed since multi-disk management Phases A-C with zero dashboard surface. Role assignment via the shared `+ Create` modal or a per-row shortcut; destructive format is a per-row action gated by `confirm()`, `confirm_disk_name` auto-filled from the already-unambiguous row (mirroring `kanxeoctl disks format`'s own reasoning) | Accepted |
| [0141](0141-multi-disk-storage-placement.md) | Multi-disk storage placement: 3 new disk roles (`state-storage`, `rebuildable-storage`, `log-storage`) each relocatable via a real async migration job (permission-preserving tree copy, single-threaded-reactor-safe atomic repoint); `backup` role goes from an inert label to a real scheduled-snapshot destination scoped to state-storage only. Requires a prerequisite layout-grouping refactor (today's paths are completely flat) with an automatic, tested upgrade path for already-installed boxes | Accepted |
| [0142](0142-disk-health-passthrough-and-io-stats.md) | Disk mount state becomes persisted (fs_type remembered) with auto-remount on startup, closing ADR-0140's own found-live "sda loses its mount across a reboot" gap; a role-less disk becomes directly grantable to a container as a raw block device via the existing `--device=` mechanism (`bus[4]`->`bus[8]`, `"disk:<name>"` ids); `GET /disks` gains real per-disk I/O (`/sys/block/<dev>/stat`) and capacity stats; container-storage becomes migratable after creation too, via the same job mechanism ADR-0141 built for the other three storage kinds | Accepted |
| [0143](0143-container-dns-servers-field.md) | New optional `dns_servers` field on `POST /containers` (0-3 IPv4 addresses, staged as a real `/etc/resolv.conf` into the container's own overlay before `execve()`) -- closes a real, previously-undiscovered gap: a container had no DNS resolution capability from Kanxeo at all before this. Deliberately explicit, no automatic wiring to a registered internal `.internal`-zone DNS server, the same no-magic posture ADR-0076 already established for the host's own outbound resolution | Accepted |
| [0144](0144-host-authentication-and-real-ldap.md) | Real host authentication (`kanxeod`'s API had none before this): write operations require a configurable admin-group membership check, backed by either a live LDAP bind+search against `glauth` (hand-rolled minimal client, no `libldap` dependency) or a local in-process check against the same underlying user/group records -- one directory, two interchangeable backends. Password storage moves from unsalted SHA256 to a vendored, audited bcrypt implementation; real secondary-group membership added. Container-level SSH auth becomes real LDAP too (OpenLDAP client + PAM + `nss_ldap`/`pam_ldap`, `openssh` rebuilt with PAM, live `AuthorizedKeysCommand` key lookup), replacing the prior file-rendering SSH-target mechanism | Accepted |
| [0145](0145-retire-file-rendered-ssh-target-sync.md) | Completes ADR-0144's own already-stated intent: once real live-LDAP SSH login (task #838) was verified end to end, the older file-rendered SSH-target mechanism (task #731, `POST /v1/ldap/ssh-targets`) was removed outright -- daemon code, API endpoints, CLI commands, all gone, not deprecated in place. No migration path needed: this project's one real deployment had already moved its only registered container off the old mechanism first | Accepted |
| [0146](0146-ldap-startup-resync-and-break-glass-recovery.md) | Root-causes and fixes a real total-lockout incident caused by activating host-auth write-gating: `ldap_record_sync_all()` now runs at daemon startup (mirroring ADR-0091's identical DNS fix, never mirrored to LDAP before), so a rebooted LDAP server's config is never silently empty. Also adds `kanxeo-recover`, a deliberately tiny break-glass boot tool (second GRUB entry on the installer media) that resets only `admin_groups` on an already-installed system's persisted host-auth config, gated by hypervisor/console access plus a typed confirmation | Accepted |
| [0147](0147-ldap-user-group-rename.md) | `PUT /v1/ldap/users\|groups/{name}` gains real rename support (a body `name` distinct from the URL path's own `{name}`, previously discarded entirely) -- a group rename carries one deliberate cascade: if the old name is currently in `hostauth-config`'s own `admin_groups`, that list follows the rename automatically, so a renamed admin group can never silently drop out of write-gating (the same class of incident ADR-0146 closed once already, a different cause) | Accepted |
| [0148](0148-glauth-basedn-daemon-managed.md) | glauth's own `baseDN` (previously a fourth independently-typed copy of `hostauth-config`'s `ldap_base_dn`) becomes daemon-managed for every registered LDAP server -- rewritten in place on each config sync, everything else in the operator-authored prefix untouched. Client-side files (`nslcd.conf` etc. in arbitrary containers) stay deliberately, permanently manual -- documented explicitly as a real boundary, not silently missing | Accepted |
| [0149](0149-unified-recipes-git-layout.md) | `pkg/recipes/` + `pkg/image-recipes/` unified into one top-level `recipes/package/` + `recipes/image/` git layout, both `<name>/<version>/build.sh` -- image recipe versions track the *recipe's own* revision history (a new package list is a new version), a separate axis from an image's content-addressed build version (ADR-0108); the daemon's own unversioned image-recipe API is unchanged (ADR-0123). `pkg sync` extended to walk and publish both kinds, picking the highest image recipe version per name | Accepted |
| [0150](0150-container-devpts-mount.md) | Every container now gets a real `devpts` mount at `/dev/pts` (`mountns_pivot()`, fresh on every start, same as `/proc`/`/sys`/`/run`) and a `/dev/ptmx` symlink (`pkg_seed_image_baseline()`, self-heals into existing images on their next install) -- fixes a real "PTY allocation request failed" hit interactively SSHing into jumpbox1, the same devpts-missing bug class task #764 already fixed once for the host's own console-exec feature, one namespace layer deeper | Accepted |
| [0151](0151-container-recipes.md) | Container recipes: a third recipe kind (`recipes/container/<name>/<version>/container.json`) alongside package/image, content is a real `POST /v1/containers` body applied via the identical `handle_create()` code path. `{{SECRET:KEY}}` tokens in staged file content are substituted at apply time from the request's own `secrets` object (correctly JSON-escaped), so a real credential never has to be committed to git | Accepted |
| [0152](0152-hostauth-session-introspection.md) | Host-auth session listing (`GET /v1/system/hostauth/sessions`, never the raw token) and revocation, deliberately per-username not per-session (`DELETE .../sessions/{username}`, "log out everywhere") since the only real per-session identifier is the token itself, never surfaced past its one-time login response. Caught and fixed a real off-by-one route-matching bug before it shipped | Accepted |
| [0153](0153-container-file-live-update.md) | `PUT /v1/containers/{name}/files?path=...` writes/overwrites one file inside an already-existing container (running, via `/proc/<pid>/root` + overlay copy-up, or stopped, direct to `upper/`) without a recreate -- deliberately live and ephemeral, never persisted into the container's own `files[]` body; the durable "survive a recreate" path stays a container recipe (ADR-0151). Caught and fixed a real use-after-free (JSON-owned content pointer used after `json_free()`) before it shipped | Accepted |
| [0154](0154-host-tools-mksquashfs-ld-library-path.md) | `mkbootroot`'s server-side `mksquashfs` invocation now sets `LD_LIBRARY_PATH` when sourced from a `kanxeo-hosttools` image -- a real, previously-undiscovered bug found on the very first real end-to-end use of that mechanism: with no library search path wired, `mksquashfs` silently linked against whatever `liblzma.so.5` the bare host happened to already have instead of the one actually built for it, producing a corrupt (but exit-0) squashfs | Accepted |
| [0155](0155-baseline-reseed-manifest-hash-dedup-gap.md) | Documents a real, permanent property of ADR-0108's manifest-hash image versioning found while closing task #865: `pkg_seed_image_baseline()`'s own "self-heals on its very next install" claim only holds when the resulting package manifest is genuinely new -- a same-manifest reinstall cycle dedupes straight back to the pre-existing directory for that hash, discarding the freshly re-seeded tree entirely. No code fix (a real, load-bearing property of the dedup, not a bug); the practical unblock is installing a package the image didn't already have | Accepted |
| [0156](0156-live-container-network-attach-detach.md) | `POST`/`DELETE /v1/containers/{name}/networks[/...]` attach/detach a network on an already-running container, live and ephemeral (never touching the persisted definition, ADR-0153's own posture) -- a real veth pair plus a `setns()` dance mirroring `container_net_host_attach_interfaces()`'s already-proven pattern. `cmd` updates without a recreate are explicitly ruled out, permanently: killing a container's init necessarily destroys its PID namespace (a real kernel invariant, not a missing feature) | Accepted |
| [0157](0157-parallel-package-builds-design.md) | Design (task #841, configurable concurrent package builds, default 10) plus Phase 1 implementation: build-transient fields moved onto the already-existing `pkg_entry` table rather than a new parallel array, plus a small new `pkg_chain` table for in-flight dependency chains; no new synchronization needed around the shared `g_pkgbuild_rootfs` merge-back since this daemon's single-threaded, run-to-completion event loop already serializes it for free. Effective concurrency limit still fixed at 1 (zero behavior change) -- phases 2-4 (raising the limit, config surface, per-build container identity) remain open | Accepted |
| [0158](0158-partition-level-disk-management.md) | Task #844: `disk_enumerate()` now reports partitions (not just whole disks), so `diskrole.c`/`diskformat.c` work on a partition with zero code changes of their own; a new `diskpart.c` adds synchronous, `sfdisk`-scripted create-partition-table/add-partition/delete-partition primitives with the same double-confirmation posture as `disks format`. Also fixes a real, previously-undiscovered `DISK_ENUM_MAX` silent-truncation gap found live while testing | Accepted |
| [0159](0159-api-driven-kernel-module-management.md) | REST-driven kernel module management, extending ADR-0061 (which already builds/stages the curated `=m` set + real `kmod`, but has zero API surface). Phase A wraps real `modprobe`/`rmmod`/`/proc/modules` for load/unload/list, with per-load and persisted-default module options and a REST-managed boot autoload list. Phase B (an additional in-tree module) reuses the already-proven whole-kernel `kernel.recipe` hostbuild + existing A/B cutover (a new REST-supplied extra-symbols parameter on the same recipe) instead of a new persistent build-tree mechanism, after an initial draft's more complex design was simplified per direct "keep the mechanics simple" feedback; genuine out-of-tree (third-party) support is explicitly left undesigned until a real need for it shows up. Every open question resolved directly with the user; Phase A implemented (task #882), Phase B/C not yet started | Accepted |
| [0160](0160-host-level-sysctl-api.md) | A new host-level `/proc/sys` REST surface (`GET`/`PUT /v1/system/sysctl/{key}`) -- distinct from the existing, unmodified, create-time-only per-container `--sysctl=` (`net.*`-only). Fully open passthrough (host-auth write-gating only, no allowlist, by explicit request), generic multi-value-key typing (any multi-token value read/written as a JSON array, no curated per-key schema), persist-by-default with per-call opt-out, a persisted boot-apply list applied between ADR-0061's hardware-detection modules and the management-network bootstrap. Every open question resolved directly with the user; implemented (task #881) | Accepted |
| [0161](0161-usb-hotplug-and-composite-device-extension.md) | Extends (doesn't replace) `device.c`/`devicemap.c` for USB, phased A-D, shipping as one combined deliverable: (A) composite-device interface metadata; (B) an opt-in `optional: true` device entry, closing a found prerequisite gap (container creation hard-fails today on any not-currently-present device reference); (C) a `NETLINK_KOBJECT_UEVENT` listener modeled on `start_kmsg_watch()`, logging every relevant hotplug event; (D) live attach/detach (mirrors ADR-0156's network shape), rejecting ambiguous multi-container contention rather than picking arbitrarily. Phase D's own live cgroup-program update was empirically tested with a real standalone probe before this document was finalized -- the result simplified the design: no `BPF_F_REPLACE`/`ALLOW_MULTI` needed, a plain re-`ATTACH` (this project's own existing, unmodified `container_dev_bpf_attach()` call shape) already atomically replaces a live program, confirmed against 2,000,000 concurrent access attempts spanning the swap. Every open question resolved directly with the user; implementation not yet started | Accepted |
