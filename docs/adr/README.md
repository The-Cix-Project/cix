# Architecture Decision Records

See [0000-adr-process.md](0000-adr-process.md) for what an ADR is for and how it's kept here (short-lived reasoning that lives forever, append-only, distinct from `docs/roadmap/ROADMAP.md`'s what-shipped narrative).

| ADR | Title | Status |
|---|---|---|
| [0000](0000-adr-process.md) | Recording architecture decisions | Accepted |
| [0001](0001-tcc-exclusive-toolchain.md) | TCC exclusively, dynamically linked against system glibc | Accepted |
| [0002](0002-raw-syscalls-for-missing-glibc-wrappers.md) | Raw syscalls where glibc has no wrapper | Accepted |
| [0003](0003-clone3-atomic-cgroup-placement.md) | clone3 + CLONE_INTO_CGROUP + CLONE_PIDFD over legacy clone() | Accepted |
| [0004](0004-overlayfs-dedicated-lowerdir.md) | OverlayFS lowerdir is a dedicated, purpose-built tree | Accepted |
| [0005](0005-api-first-mandate.md) | API-First Mandate: the REST daemon is the only path to the runtime | Accepted |
| [0006](0006-openapi-spec-format.md) | OpenAPI 3.0 YAML as the API spec format | Accepted |
| [0007](0007-hand-rolled-daemon-no-external-libs.md) | Hand-rolled HTTP/JSON/reactor in the daemon, no external libraries | Accepted |
| [0008](0008-tcc-pragma-pack-for-kernel-abi-structs.md) | Use #pragma pack, not __attribute__((packed)), for kernel-ABI structs under TCC | Accepted |
| [0009](0009-cloexec-daemon-fds-before-clone3.md) | CLOEXEC every daemon-owned fd before any clone3() call | Accepted |
| [0010](0010-vanilla-web-dashboard-no-build-step.md) | Vanilla HTML/CSS/JS dashboard, no framework, no build step, served by cixd | Accepted |
| [0011](0011-rtnetlink-control-plane-over-kernel-bridge.md) | rtnetlink control plane over the kernel's own bridge/veth, not a userspace switch | Accepted |
| [0012](0012-networks-json-persistence.md) | Atomically-rewritten flat file for network persistence, not a database | Accepted |
| [0013](0013-proc-pid-root-for-live-container-file-writes.md) | /proc/<pid>/root/ for writing into a running container's filesystem, not raw upperdir or setns() | Accepted |
| [0014](0014-squashfs-ab-root-with-native-boot-counting.md) | Read-only squashfs A/B root with systemd-boot's native boot counting, not a writable root or hand-rolled rollback | Accepted |
| [0015](0015-shim-mok-secure-boot-signing.md) | shim + MOK enrollment for Secure Boot on the installed system, installer media stays unsigned | Accepted |
| [0016](0016-reboot-syscall-for-cixd-shutdown.md) | cixd calls reboot(2) as PID 1, exposed via REST, SIGTERM/SIGINT default to poweroff | Accepted |
| [0017](0017-ebpf-cgroup-device-filter-for-hardware-passthrough.md) | BPF_CGROUP_DEVICE for device passthrough enforcement, not a blanket eBPF ban | Accepted |
| [0018](0018-containers-partition-for-base-dir-persistence.md) | The real cix-containers partition backs BASE_DIR, not a fresh tmpfs every boot | Accepted |
| [0019](0019-runtime-libs-seeded-at-install-time.md) | The shared "base" image's C runtime is seeded at install time, not by pkg or at boot | Accepted |
| [0020](0020-per-image-pkg-install-compound-key.md) | pkg install targets an explicit image, tracked by a (name, image) compound key | Accepted |
| [0021](0021-explicit-network-ip-override.md) | A container's network attachment IP can be explicitly chosen, not only auto-allocated | Accepted |
| [0022](0022-real-nic-passthrough-netns-move.md) | Real network interface passthrough via direct netns move, with fd-anchored teardown | Accepted |
| [0023](0023-per-image-runtime-seeding.md) | The C runtime is seeded into every image, not only "base" | Accepted |
| [0024](0024-image-lifecycle-endpoints.md) | Image lifecycle as a filesystem-backed REST resource, no separate persistence | Accepted |
| [0025](0025-persisted-auto-restarting-containers.md) | Persisted, auto-restarting containers (`restart: "always"`, `depends_on`) | Accepted |
| [0026](0026-tcp-readiness-checks-for-depends-on.md) | TCP readiness checks for `depends_on` | Accepted |
| [0027](0027-restart-policy-expansion-and-backoff.md) | Restart policy expansion (`on-failure`, `unless-stopped`) and crash-restart backoff | Accepted |
| [0028](0028-gpu-passthrough-grouped-device-grants.md) | GPU passthrough: grouped multi-node device grants | Accepted |
| [0029](0029-gpu-kernel-driver-firmware-and-kfd.md) | GPU kernel driver, firmware staging, and KFD discovery | Accepted |
| [0030](0030-per-container-config-files-and-sysctls.md) | Per-container config files and generalized sysctls | Accepted |
| [0031](0031-host-and-package-update-mechanism.md) | Host OS update mechanism (write to inactive A/B slot), and automatic package updates | Accepted |
| [0032](0032-per-slot-kernel-updates.md) | Per-slot kernel files, extending POST /system/update to cover bzImage | Accepted |
| [0033](0033-platform-state-backup-restore.md) | Platform state backup/restore -- configuration only, not workload data, never PKI | Accepted |
| [0034](0034-console-login-via-supervised-cixctl.md) | Console login via a cixd-supervised cixctl, not a general shell | Accepted |
| [0035](0035-portable-toolchain-artifact-for-pkg-bootstrap.md) | Portable toolchain artifact for pkg bootstrap, not just a live-host copy | Accepted |
| [0036](0036-multi-source-package-recipes.md) | Multi-source package recipes | Accepted |
| [0037](0037-network-gateway-optional.md) | A network's host-owned gateway becomes optional, default flips to gateway-less | Accepted |
| [0038](0038-vlan-and-physical-nic-bridge-attachment.md) | VLAN via 802.1q sub-interfaces, physical NIC attachment via the existing rtnl_link_set_master | Accepted |
| [0039](0039-package-recipes-seeded-at-install-time.md) | Package recipes seeded at install time, same mechanism as ADR-0019 | Superseded by [0040](0040-package-recipes-managed-via-rest-api.md) |
| [0040](0040-package-recipes-managed-via-rest-api.md) | Package recipes are managed via a real REST API, not baked into the installer ISO | Accepted |
| [0041](0041-container-image-baseline-fhs-layout.md) | Container images get a real, fixed baseline FHS layout at seed time | Accepted |
| [0042](0042-installer-dual-console-pty-relay.md) | The installer relays interactive I/O through a PTY across both consoles at once | Accepted |
| [0043](0043-container-console-exec-websocket.md) | An interactive shell into a running container, over a hand-rolled WebSocket | Accepted |
| [0044](0044-daemon-data-dir-test-isolation.md) | `cixd --data-dir=` for test/production state isolation | Accepted |
| [0045](0045-container-start-pause-lifecycle.md) | Container start/pause: cgroup-freeze lifecycle completeness | Accepted |
| [0046](0046-site-scoped-dns-pki-naming.md) | Site-scoped DNS/PKI naming: a real, configurable convenience, not enforcement | Accepted |
| [0047](0047-two-tier-pki-intermediate-ca.md) | Two-tier PKI: an intermediate CA for day-to-day signing | Accepted |
| [0048](0048-persistent-device-name-mappings.md) | Persistent device name mappings: exact vs. vendor/model resolution | Accepted |
| [0049](0049-pki-ca-reset-regeneration.md) | PKI CA reset/regeneration: reissue-in-place, not destroy | Accepted |
| [0050](0050-auto-issued-host-pki-cert.md) | Auto-issued host PKI cert: a fixed record name, not the FQDN itself | Accepted |
| [0051](0051-ca-trust-staged-into-images.md) | CA trust chain staged into every image at creation time | Accepted |
| [0052](0052-server-side-default-name-qualification.md) | Server-side default DNS/PKI name qualification (revises ADR-0046) | Accepted |
| [0053](0053-auto-maintained-instance-dns-record.md) | Auto-maintained instance DNS record | Accepted |
| [0054](0054-host-side-container-stats.md) | Host-side per-container stats: raw point-in-time snapshot, no server history | Accepted |
| [0055](0055-container-file-read-endpoint.md) | Container file-read REST endpoint: raw bytes, running-vs-exited path resolution | Accepted |
| [0056](0056-hostbuild-artifact-mechanism.md) | Hostbuild artifact mechanism: a second pkg-install mode that harvests a standalone artifact instead of merging into an image | Accepted |
| [0057](0057-self-hosted-toolchain-and-control-plane-rebuild.md) | Self-hosted toolchain and control-plane rebuild: tcc.recipe, cix.recipe, server-side mkbootroot | Accepted |
| [0058](0058-host-management-network-unification.md) | The host's own management address becomes a real, API-managed network | Accepted |
| [0059](0059-openssl-https-listener.md) | OpenSSL-backed HTTPS listener, reusing the PKI host cert, via a small TLS side-table | Accepted |
| [0060](0060-cpu-bandwidth-and-affinity-api-exposure.md) | Expose cpu.max and cpuset.cpus through the API/CLI, no reinterpretation layer | Accepted |
| [0061](0061-kernel-module-loading.md) | Kernel module loading: root-critical drivers stay built-in, only network/USB become modules | Accepted |
| [0062](0062-ext4-project-disk-quotas.md) | Disk quotas: real ext4 project-quota enforcement, permanent project-id assignment | Accepted |
| [0063](0063-iso-self-build-toolchain.md) | ISO self-build: a real, from-source GRUB2/xorriso/mtools/sbsigntools toolchain | Accepted |
| [0064](0064-rest-driven-iso-assembly.md) | REST-driven ISO assembly: closing the API-First Mandate gap ADR-0063 left open | Accepted |
| [0065](0065-pkg-bootstrap-url-fetch.md) | pkg bootstrap URL-fetch: closing a real gap in ADR-0031's own "operator scp's it" assumption | Accepted |
| [0066](0066-kernel-routing-table-diagnostic.md) | A real kernel routing-table diagnostic: closing the last blind spot in "the daemon is the only way to inspect a running box" | Accepted |
| [0067](0067-network-address-field-rename.md) | Rename a network's `gateway`/`has_gateway` field to `address`/`has_address` | Accepted |
| [0068](0068-dedicated-daemon-bind-ip.md) | A dedicated daemon bind IP, decoupled from the management network's own address | Superseded by ADR-0287 |
| [0069](0069-host-swap-file.md) | A single, on-demand host swap file: direct swap-header write, no `mkswap` dependency | Accepted |
| [0070](0070-consolidated-log-store.md) | A consolidated, API-accessible, size-capped log store: kernel dmesg + cixd diagnostics + per-request audit trail | Accepted |
| [0071](0071-disk-format-mount.md) | Disk format + mount as a separate, explicitly-confirmed action; a non-execve()ing job child for its two sequential steps | Accepted |
| [0072](0072-log-message-cap-and-min-level.md) | Log message cap raised 512->4096, build-output capture switched head->tail, settable write-time minimum severity | Accepted |
| [0073](0073-host-stats-endpoint.md) | Host-wide stats endpoint (GET /v1/system/stats): load/CPU/memory/disk/network, mirroring per-container stats | Accepted |
| [0074](0074-pressure-stall-information.md) | Pressure-stall information (PSI) in host and per-container stats: cpu/io/memory.pressure | Accepted |
| [0075](0075-icmp-reachability-endpoint.md) | Reachability endpoint (GET/POST /v1/system/ping): real, hand-rolled ICMP echo | Accepted |
| [0076](0076-host-dns-resolver-config.md) | Host DNS resolver config (GET/PUT /v1/system/resolv) + a real /etc/resolv.conf bind-mount fix | Accepted |
| [0077](0077-health-boot-identity-split.md) | Split GET /health into liveness + GET /system/boot identity | Accepted |
| [0078](0078-from-source-host-tools-bootstrap.md) | From-source host tools bootstrap (mkbootroot.c's shelled-out binaries) | Accepted |
| [0079](0079-cgroup-controller-delegation.md) | cgroup v2 controller delegation at daemon startup (real-PID-1/no-systemd container-create 500 fix) | Accepted |
| [0080](0080-container-diagnostics-visibility.md) | Container creation/exit diagnostics visibility -- real errno text, not just numeric codes | Accepted |
| [0081](0081-cgroup-controller-delegation-atomic-write-fix.md) | cgroup controller delegation: fix the atomic-write regression ADR-0079 introduced | Accepted |
| [0082](0082-kernel-smp-missing.md) | CONFIG_SMP was never enabled -- uniprocessor kernel, and the real root cause of the cpuset gap | Accepted |
| [0083](0083-mkbootroot-ld-linux-second-copy.md) | mkbootroot's own control-plane root was missing the second ld-linux copy | Accepted |
| [0084](0084-mkbootroot-mksquashfs-unreachable-on-real-host.md) | mkbootroot's own mksquashfs exec target was never reachable on a real installed host | Accepted |
| [0085](0085-mkbootroot-source-paths-not-merged-usr.md) | The real, final root cause: mkbootroot's own source reads assumed a merged-usr host | Accepted |
| [0086](0086-pkgbuild-stop-bypasses-pkg-completion.md) | Stopping __pkgbuild via POST .../stop left pkg.c's job lock stuck forever | Accepted |
| [0087](0087-pkg-build-output-pipe-deadlock.md) | Build-output capture pipes were drained once, after exit, deadlocking any sufficiently verbose build | Accepted |
| [0088](0088-kernel-veth-missing.md) | CONFIG_VETH was never enabled -- every container network attachment on a real box has always failed | Accepted |
| [0089](0089-root-netns-ip-forward-missing.md) | root-netns net.ipv4.ip_forward was never enabled -- no container could ever originate a connection past the host | Accepted |
| [0090](0090-container-create-log-use-after-free.md) | container-create failure logging used a dangling JSON-tree pointer | Accepted |
| [0091](0091-dns-server-binding-not-persisted.md) | DNS server bindings were never persisted, silently lost on every restart | Accepted |
| [0092](0092-dns-register-missing-site-qualification.md) | auto-registered container DNS records were never site-qualified, unlike manual ones | Accepted |
| [0093](0093-extract-tarball-assumed-wrapping-directory.md) | extract_tarball() assumed every source tarball has a wrapping directory | Accepted |
| [0094](0094-hostbuild-upgrade-flag.md) | pkg hostbuild had no upgrade/force path, a bare permanent 409 | Accepted |
| [0095](0095-update-one-sided-footgun.md) | POST /system/update's one-sided image_path/kernel_path footgun | Accepted |
| [0096](0096-pkg-fetch-diagnostic-capture.md) | pkg fetch failures reported only a bare curl exit code, curl's own real error text was discarded | Accepted |
| [0097](0097-mkbootroot-missing-ca-bundle.md) | mkbootroot never staged a CA certificate bundle -- every host-side HTTPS pkg_source fetch had no way to verify a TLS cert | Accepted |
| [0098](0098-tarball-listing-truncation-false-mismatch.md) | tarball_has_common_top_dir() false-positived "no common top dir" when its own 64KB capture truncated mid-line | Accepted |
| [0099](0099-disk-mount-ground-truth.md) | GET /disks reports real mount status from /proc/mounts, not diskformat.c's own ephemeral job history | Accepted |
| [0100](0100-container-cmd-visibility.md) | GET /containers echoes cmd, sourced from registry_entry (live) or the persisted body (stopped) | Accepted |
| [0101](0101-pkg-build-log-live-tail.md) | GET /v1/pkg/build/log live-tails an in-flight build's own output, a one-way WS relay distinct from the console's exec/PTY session | Accepted |
| [0102](0102-per-container-disk-selection.md) | Per-container disk selection at creation time (Phase D) -- POST /containers' new "disk" field, gated on the container-storage role | Accepted |
| [0103](0103-btrfs-quota-backend.md) | Real btrfs qgroup-based disk quotas as a second backend alongside ext4 project quotas, closing the --disk-quota= filesystem gap | Accepted |
| [0104](0104-btrfs-disk-format.md) | fs_type on the disk-format REST endpoint -- real btrfs disk formatting alongside ext4 (btrfs-progs.recipe, libblkid.recipe) | Accepted |
| [0105](0105-bootroot-assembly-freshness.md) | Real generation-counter freshness tracking for `pkg hostbuild cix --deploy`, closing the stale-artifact silent-redeploy gap | Accepted |
| [0106](0106-container-delete-disk-cleanup.md) | DELETE /containers actually removes on-disk upper/work/merged state, unmounting the overlay first | Accepted |
| [0107](0107-package-image-versioning.md) | Package + image versioning model: version-keyed recipes, per-image pinned/rolling manifests, per-version immutable image rootfs | Accepted |
| [0108](0108-image-version-content-hash.md) | Image versions are a manifest-content hash (sha256 of resolved package@version list), not a sequential integer; explicit created_at tracking for both image and recipe versions | Accepted |
| [0109](0109-ldap-redesign.md) | LDAP redesign: glauth, config-render write-through (mirroring DNS, not a second SQLite-owned datastore), and owner-tracked automatic service-account provisioning | Accepted |
| [0110](0110-ntp-host-clock-sync.md) | NTP: hand-rolled SNTP client for the host clock (clock_settime() needs host-namespace CAP_SYS_TIME, no container can do it), plus container time-source registration mirroring DNS/LDAP | Accepted |
| [0111](0111-ssh-ldap-account-sync.md) | Jump box SSH auth backed by LDAP: rendered Unix accounts (passwd/group/shadow/authorized_keys), not real NSS/PAM-over-LDAP | Accepted, one decision superseded by ADR-0296 |
| [0112](0112-container-output-capture.md) | Opt-in stdout/stderr capture for ordinary containers, reusing the existing build-container pipe/epoll-drain mechanism -- closes the diagnostic gap named by tasks #731 and #747 | Accepted |
| [0113](0113-glauth-listener-mystery-resolved.md) | glauth's "listener mystery" resolved for real: an out-of-range diagnostic IP colliding with a real LAN device, plus a genuine missing CONFIG_INOTIFY_USER kernel gap -- neither a Cix nor a glauth defect | Accepted |
| [0114](0114-kernel-config-gap-config-seccomp.md) | Fourth allnoconfig-starting kernel config gap: CONFIG_SECCOMP was never enabled, so every SSH login's own preauth sandbox setup failed and reset the connection | Accepted |
| [0115](0115-delete-crashed-container-disk-cleanup.md) | DELETE's task #738 disk cleanup never ran for a crashed-but-defined container, letting a stale upperdir's wrong file permissions crash-loop a restart:"always" container forever under a reused name | Accepted |
| [0116](0116-json-parser-u-escape-support.md) | json_parse() gains narrow \uXXXX support, closing a write/parse asymmetry that silently broke cixctl ps whenever a container's captured_output held a colorized program's ANSI escapes | Accepted |
| [0117](0117-dns-record-qualify-on-read.md) | DNS record GET/PUT/DELETE now qualify a bare name the same way POST already does -- a record created with --name=foo could only ever be looked back up by its full site-qualified FQDN | Accepted |
| [0118](0118-boot-init-devpts-mount.md) | boot_init() mounts /dev/pts: the real root cause of every live console/exec 500 on an installed box -- cix-install.c's own devpts mount was never carried over to cixd's own separate real-PID-1 boot path | Accepted |
| [0119](0119-container-run-tmpfs.md) | /run is a fresh tmpfs on every container start, not overlay-persisted storage -- root cause of chronyd's respawn self-collision (stale PID-1 pidfile) that made ntp-1/ntp-2 never survive a reboot | Accepted |
| [0120](0120-pkg-redesign-part1-naming-and-backup-fix.md) | pkg/ redesign Part 1: recipe.sh -> build.sh rename, plus a real backup/restore regression found and fixed along the way (system backup had included zero recipes since ADR-0107's version-keyed layout landed) | Accepted |
| [0121](0121-pkg-redesign-part2-configurable-repo-and-sync.md) | pkg/ redesign Part 2: configurable recipe repo (gitea/github/gitlab) + merge/additive `pkg sync`, real-only-for-gitea fetch verification | Accepted |
| [0122](0122-pkg-redesign-part3-artifact-cache-and-server.md) | pkg/ redesign Part 3: local build-artifact cache with LRU eviction + a plain-HTTP (never git) precompiled-artifact server, checksum-verified against the recipe's own git-tracked hash | Accepted |
| [0123](0123-pkg-redesign-part4-image-recipes-and-artifact.md) | pkg/ redesign Part 4: declarative image recipes (bulk-declare a manifest) + a whole-rootfs artifact fast path for fully-pinned recipes, reusing Part 3's plain-HTTP server under a distinct URL prefix | Accepted |
| [0124](0124-pkg-redesign-part5-rolling-containers-and-restart-jitter.md) | pkg/ redesign Part 5: opt-in per-container `follow_rolling`, reconciled after every pkg-job completion, with a configurable jittered-restart window to spread out simultaneous restarts; found and fixed a real timerfd zero-`it_value`-disarms-not-fires-immediately bug along the way | Accepted |
| [0125](0125-pkg-delete-failed-state-and-jitter-override.md) | Part 5 follow-up: `pkg_delete()` now clears a permanently-`PKG_STATE_FAILED` entry directly (nothing was ever really merged into an image, so no image version is produced) alongside the existing `PKG_STATE_INSTALLED` uninstall path; `follow_rolling` gains a per-container `follow_rolling_jitter_seconds` override of the daemon-wide default | Accepted |
| [0126](0126-transparent-container-log-capture.md) | Logging/web-UI epic Part 1: every container's stdout/stderr is now unconditionally captured into the existing consolidated log store (not opt-in, not a second store), with new `container=`/`regex=` filters; `capture_output` narrows to controlling only the separate per-container `captured_output` tail; also fixes a live TLS-handshake-error console-spam bug found along the way | Accepted |
| [0127](0127-syslog-forward-targets.md) | Logging/web-UI epic Part 2: optional, redundant `syslog-1`/`syslog-2` forward targets -- `POST`/`GET`/`DELETE /v1/syslog/targets` registers a running container (mirrors NTP server registration), every container-sourced log line is also sent as a real RFC 3164 UDP datagram, alongside (never instead of) the consolidated log store, which stays the one source of truth | Accepted |
| [0128](0128-host-owner-sentinel.md) | Logging/web-UI epic Part 5: the daemon's own auto-issued "host" PKI cert and auto-maintained instance DNS record now carry a reserved `"__host"` owner sentinel (rendered as "host (this daemon)" in CLI/web) instead of `null`, distinguishing "owned by the daemon itself" from "no owner for any other reason" | Accepted |
| [0129](0129-merged-log-panel-and-toast.md) | Logging/web-UI epic Part 3: the web dashboard's bottom action-log panel now merges server logs (kernel/cixd/audit/container) with its own client-side "web-ui" activity into one filtered stream; the Logs tree page keeps only the size-cap config; the status bar becomes a fixed-position, auto-dismissing toast that also lands in the merged log panel | Accepted |
| [0130](0130-host-stats-graphs-page.md) | Logging/web-UI epic Part 4: a new System > Server > Host Stats page graphs `GET /system/stats` (load/CPU/memory/disk/network), reusing the container Stats tab's own `drawChart()` canvas renderer -- host-side CPU% uses the classic `/proc/stat` idle-delta formula since the source data is raw jiffies, not a cgroup usage counter | Accepted |
| [0131](0131-host-process-list-and-kill.md) | Logging/web-UI epic Part 6 (final): `GET`/`DELETE /v1/system/processes` -- a real `/proc` scan of every host process, each correlated to a container (if any) via a real host ppid-chain walk against the registry's own known container root pids; kill is a real, immediate SIGKILL, refusing only pid 1 and this daemon's own pid | Accepted |
| [0132](0132-log-panel-fixes-and-cli-polish.md) | Real-world fixes after the logging epic shipped: log panel fixed-size CSS bug (`min-height: 0`) + batched DOM rendering + generalizing the audit log's `GET /health` exclusion to every `GET` (fixes both the visual growth and the dashboard slowdown, same root cause); `resolv_set()` switched from a rename-based to an in-place write so `PUT /v1/system/resolv` actually stays live across the bind-mounted `/etc/resolv.conf` instead of silently detaching until the next reboot; dynamic `<instance_name>>` shell prompt; `process ls` restyled to match `ps`; new `container ls` synonym | Accepted |
| [0133](0133-lenient-repo-url-parsing.md) | `parse_repo_url()` (pkg repo sync) now reads only the first two path segments as owner/repo instead of splitting at the last slash -- a real forge browse URL pasted verbatim (e.g. gitea's own `.../owner/repo/src/branch/<ref>/<path>`) now resolves correctly instead of silently mis-parsing into a garbage owner and an opaque 404 | Accepted |
| [0134](0134-tls-handshake-throttling.md) | Per-source-IP throttling for repeated failed HTTPS handshakes: the peer IP is now logged, a source that fails enough within a window is refused outright on both listeners, loopback is never throttled (avoids locking out the daemon's own local admin access), fully API-driven and configurable | Accepted |
| [0135](0135-tls-throttle-log-rate-limit-and-ca-download.md) | ADR-0134 follow-up: the real flood source turned out to be the user's own untrusted-cert browser, not a hostile source -- rate-limited the handshake-failure log line independently of the block threshold, and added a CA cert download button + platform-specific trust instructions | Accepted |
| [0136](0136-https-listener-serves-full-chain.md) | ADR-0135 follow-up: the HTTPS listener was only ever sending its leaf cert during the handshake, never the intermediate CA, so a client trusting only the root (the documented workflow) could never actually validate the chain on a box with an intermediate bootstrapped -- fixed via `SSL_CTX_add_extra_chain_cert()`; also renamed the CA download button's file from `.pem` to `.crt` | Accepted |
| [0137](0137-tls-throttle-block-scoped-to-https-listener.md) | ADR-0134 revisited: a source blocked for repeated failed HTTPS handshakes no longer also loses plain HTTP access -- only a failed TLS handshake can ever cause a block, so enforcing it against the unrelated listener stranded a legitimate fallback path (found live: the user's own untrusted-cert desktop retrying HTTPS lost HTTP access too) | Accepted |
| [0138](0138-web-dashboard-ia-rework.md) | Web dashboard information-architecture rework: System > Server (10 leaves) split into Host/Monitoring/Maintenance sized like its siblings; Software split into Catalog/Build Pipeline; PKI's overloaded "Root CA" leaf split into Root CA + Intermediate CA; NTP's Status+Time merged into Time & Sync; Route creation moved to the shared `+ Create` modal; Container detail's Summary absorbed Stats and the content-free Backup tab was removed, Summary became the default tab; Network detail gained a "Containers on this network" table (a real, previously-unfilled data gap) -- zero API/daemon changes, pure client-side reorganization | Accepted |
| [0139](0139-web-dashboard-theme-and-last-view-memory.md) | Web dashboard: a header button cycles Auto/Light/Dark (new `:root[data-theme]` CSS overrides layered on the existing `prefers-color-scheme` support, no flash on load via an inline bootstrap script), and a bare load with no hash now restores the last-viewed page instead of always starting at Containers -- tree collapse memory was investigated and found already correct, no code change needed there | Accepted |
| [0140](0140-web-dashboard-disk-management.md) | Web dashboard: new System > Host > Disks page closes a real API/UI parity gap -- `GET /disks`/`diskroles`/`disks/{name}/format` had existed since multi-disk management Phases A-C with zero dashboard surface. Role assignment via the shared `+ Create` modal or a per-row shortcut; destructive format is a per-row action gated by `confirm()`, `confirm_disk_name` auto-filled from the already-unambiguous row (mirroring `cixctl disks format`'s own reasoning) | Accepted |
| [0141](0141-multi-disk-storage-placement.md) | Multi-disk storage placement: 3 new disk roles (`state-storage`, `rebuildable-storage`, `log-storage`) each relocatable via a real async migration job (permission-preserving tree copy, single-threaded-reactor-safe atomic repoint); `backup` role goes from an inert label to a real scheduled-snapshot destination scoped to state-storage only. Requires a prerequisite layout-grouping refactor (today's paths are completely flat) with an automatic, tested upgrade path for already-installed boxes | Accepted |
| [0142](0142-disk-health-passthrough-and-io-stats.md) | Disk mount state becomes persisted (fs_type remembered) with auto-remount on startup, closing ADR-0140's own found-live "sda loses its mount across a reboot" gap; a role-less disk becomes directly grantable to a container as a raw block device via the existing `--device=` mechanism (`bus[4]`->`bus[8]`, `"disk:<name>"` ids); `GET /disks` gains real per-disk I/O (`/sys/block/<dev>/stat`) and capacity stats; container-storage becomes migratable after creation too, via the same job mechanism ADR-0141 built for the other three storage kinds | Accepted |
| [0143](0143-container-dns-servers-field.md) | New optional `dns_servers` field on `POST /containers` (0-3 IPv4 addresses, staged as a real `/etc/resolv.conf` into the container's own overlay before `execve()`) -- closes a real, previously-undiscovered gap: a container had no DNS resolution capability from Cix at all before this. Deliberately explicit, no automatic wiring to a registered internal `.internal`-zone DNS server, the same no-magic posture ADR-0076 already established for the host's own outbound resolution | Accepted, one decision superseded by ADR-0295 |
| [0144](0144-host-authentication-and-real-ldap.md) | Real host authentication (`cixd`'s API had none before this): write operations require a configurable admin-group membership check, backed by either a live LDAP bind+search against `glauth` (hand-rolled minimal client, no `libldap` dependency) or a local in-process check against the same underlying user/group records -- one directory, two interchangeable backends. Password storage moves from unsalted SHA256 to a vendored, audited bcrypt implementation; real secondary-group membership added. Container-level SSH auth becomes real LDAP too (OpenLDAP client + PAM + `nss_ldap`/`pam_ldap`, `openssh` rebuilt with PAM, live `AuthorizedKeysCommand` key lookup), replacing the prior file-rendering SSH-target mechanism | Accepted |
| [0145](0145-retire-file-rendered-ssh-target-sync.md) | Completes ADR-0144's own already-stated intent: once real live-LDAP SSH login (task #838) was verified end to end, the older file-rendered SSH-target mechanism (task #731, `POST /v1/ldap/ssh-targets`) was removed outright -- daemon code, API endpoints, CLI commands, all gone, not deprecated in place. No migration path needed: this project's one real deployment had already moved its only registered container off the old mechanism first | Accepted |
| [0146](0146-ldap-startup-resync-and-break-glass-recovery.md) | Root-causes and fixes a real total-lockout incident caused by activating host-auth write-gating: `ldap_record_sync_all()` now runs at daemon startup (mirroring ADR-0091's identical DNS fix, never mirrored to LDAP before), so a rebooted LDAP server's config is never silently empty. Also adds `cix-recover`, a deliberately tiny break-glass boot tool (second GRUB entry on the installer media) that resets only `admin_groups` on an already-installed system's persisted host-auth config, gated by hypervisor/console access plus a typed confirmation | Accepted |
| [0147](0147-ldap-user-group-rename.md) | `PUT /v1/ldap/users\|groups/{name}` gains real rename support (a body `name` distinct from the URL path's own `{name}`, previously discarded entirely) -- a group rename carries one deliberate cascade: if the old name is currently in `hostauth-config`'s own `admin_groups`, that list follows the rename automatically, so a renamed admin group can never silently drop out of write-gating (the same class of incident ADR-0146 closed once already, a different cause) | Accepted |
| [0148](0148-glauth-basedn-daemon-managed.md) | glauth's own `baseDN` (previously a fourth independently-typed copy of `hostauth-config`'s `ldap_base_dn`) becomes daemon-managed for every registered LDAP server -- rewritten in place on each config sync, everything else in the operator-authored prefix untouched. Client-side files (`nslcd.conf` etc. in arbitrary containers) stay deliberately, permanently manual -- documented explicitly as a real boundary, not silently missing | Accepted |
| [0149](0149-unified-recipes-git-layout.md) | `pkg/recipes/` + `pkg/image-recipes/` unified into one top-level `recipes/package/` + `recipes/image/` git layout, both `<name>/<version>/build.sh` -- image recipe versions track the *recipe's own* revision history (a new package list is a new version), a separate axis from an image's content-addressed build version (ADR-0108); the daemon's own unversioned image-recipe API is unchanged (ADR-0123). `pkg sync` extended to walk and publish both kinds, picking the highest image recipe version per name | Accepted |
| [0150](0150-container-devpts-mount.md) | Every container now gets a real `devpts` mount at `/dev/pts` (`mountns_pivot()`, fresh on every start, same as `/proc`/`/sys`/`/run`) and a `/dev/ptmx` symlink (`pkg_seed_image_baseline()`, self-heals into existing images on their next install) -- fixes a real "PTY allocation request failed" hit interactively SSHing into jumpbox1, the same devpts-missing bug class task #764 already fixed once for the host's own console-exec feature, one namespace layer deeper | Accepted |
| [0151](0151-container-recipes.md) | Container recipes: a third recipe kind (`recipes/container/<name>/<version>/container.json`) alongside package/image, content is a real `POST /v1/containers` body applied via the identical `handle_create()` code path. `{{SECRET:KEY}}` tokens in staged file content are substituted at apply time from the request's own `secrets` object (correctly JSON-escaped), so a real credential never has to be committed to git | Accepted |
| [0152](0152-hostauth-session-introspection.md) | Host-auth session listing (`GET /v1/system/hostauth/sessions`, never the raw token) and revocation, deliberately per-username not per-session (`DELETE .../sessions/{username}`, "log out everywhere") since the only real per-session identifier is the token itself, never surfaced past its one-time login response. Caught and fixed a real off-by-one route-matching bug before it shipped | Accepted |
| [0153](0153-container-file-live-update.md) | `PUT /v1/containers/{name}/files?path=...` writes/overwrites one file inside an already-existing container (running, via `/proc/<pid>/root` + overlay copy-up, or stopped, direct to `upper/`) without a recreate -- deliberately live and ephemeral, never persisted into the container's own `files[]` body; the durable "survive a recreate" path stays a container recipe (ADR-0151). Caught and fixed a real use-after-free (JSON-owned content pointer used after `json_free()`) before it shipped | Accepted |
| [0154](0154-host-tools-mksquashfs-ld-library-path.md) | `mkbootroot`'s server-side `mksquashfs` invocation now sets `LD_LIBRARY_PATH` when sourced from a `cix-hosttools` image -- a real, previously-undiscovered bug found on the very first real end-to-end use of that mechanism: with no library search path wired, `mksquashfs` silently linked against whatever `liblzma.so.5` the bare host happened to already have instead of the one actually built for it, producing a corrupt (but exit-0) squashfs | Accepted |
| [0155](0155-baseline-reseed-manifest-hash-dedup-gap.md) | Documents a real, permanent property of ADR-0108's manifest-hash image versioning found while closing task #865: `pkg_seed_image_baseline()`'s own "self-heals on its very next install" claim only holds when the resulting package manifest is genuinely new -- a same-manifest reinstall cycle dedupes straight back to the pre-existing directory for that hash, discarding the freshly re-seeded tree entirely. No code fix (a real, load-bearing property of the dedup, not a bug); the practical unblock is installing a package the image didn't already have | Accepted |
| [0156](0156-live-container-network-attach-detach.md) | `POST`/`DELETE /v1/containers/{name}/networks[/...]` attach/detach a network on an already-running container, live and ephemeral (never touching the persisted definition, ADR-0153's own posture) -- a real veth pair plus a `setns()` dance mirroring `container_net_host_attach_interfaces()`'s already-proven pattern. `cmd` updates without a recreate are explicitly ruled out, permanently: killing a container's init necessarily destroys its PID namespace (a real kernel invariant, not a missing feature) | Accepted |
| [0157](0157-parallel-package-builds-design.md) | Design (task #841, configurable concurrent package builds, default 10), fully implemented across all four phases: Phase 1 (build-transient fields moved onto the already-existing `pkg_entry` table rather than a new parallel array, plus a small new `pkg_chain` table for in-flight dependency chains; no new synchronization needed around the shared `g_pkgbuild_rootfs` merge-back since this daemon's single-threaded, run-to-completion event loop already serializes it for free), Phase 2 (concurrency limit raised 1->2, per-build container identity, fetch-side conn/entry threading, chain-aware build-output/live-tail WebSocket, a real 2-concurrent-build test), Phase 3 (real ceiling raised to 10, `GET`/`PUT /v1/system/pkg-build-config`, CLI/web/docs), and Phase 4 (an N-way concurrent-vs-serial stress test proving byte-identical merge-back content -- the decisive proof this design called for from the start). Task #841 is complete | Accepted |
| [0158](0158-partition-level-disk-management.md) | Task #844: `disk_enumerate()` now reports partitions (not just whole disks), so `diskrole.c`/`diskformat.c` work on a partition with zero code changes of their own; a new `diskpart.c` adds synchronous, `sfdisk`-scripted create-partition-table/add-partition/delete-partition primitives with the same double-confirmation posture as `disks format`. Also fixes a real, previously-undiscovered `DISK_ENUM_MAX` silent-truncation gap found live while testing | Accepted |
| [0159](0159-api-driven-kernel-module-management.md) | REST-driven kernel module management, extending ADR-0061 (which already builds/stages the curated `=m` set + real `kmod`, but has zero API surface). Phase A wraps real `modprobe`/`rmmod`/`/proc/modules` for load/unload/list, with per-load and persisted-default module options and a REST-managed boot autoload list. Phase B (an additional in-tree module) reuses the already-proven whole-kernel `kernel.recipe` hostbuild + existing A/B cutover (a new REST-supplied extra-symbols parameter on the same recipe) instead of a new persistent build-tree mechanism, after an initial draft's more complex design was simplified per direct "keep the mechanics simple" feedback; genuine out-of-tree (third-party) support is explicitly left undesigned until a real need for it shows up. Every open question resolved directly with the user; Phase A implemented (task #882), Phase B implemented (task #883), Phase C deliberately undesigned | Accepted |
| [0160](0160-host-level-sysctl-api.md) | A new host-level `/proc/sys` REST surface (`GET`/`PUT /v1/system/sysctl/{key}`) -- distinct from the existing, unmodified, create-time-only per-container `--sysctl=` (`net.*`-only). Fully open passthrough (host-auth write-gating only, no allowlist, by explicit request), generic multi-value-key typing (any multi-token value read/written as a JSON array, no curated per-key schema), persist-by-default with per-call opt-out, a persisted boot-apply list applied between ADR-0061's hardware-detection modules and the management-network bootstrap. Every open question resolved directly with the user; implemented (task #881) | Accepted |
| [0161](0161-usb-hotplug-and-composite-device-extension.md) | Extends (doesn't replace) `device.c`/`devicemap.c` for USB, phased A-D, shipping as one combined deliverable: (A) composite-device interface metadata; (B) an opt-in `optional: true` device entry, closing a found prerequisite gap (container creation hard-fails today on any not-currently-present device reference); (C) a `NETLINK_KOBJECT_UEVENT` listener modeled on `start_kmsg_watch()`, logging every relevant hotplug event; (D) live attach/detach (mirrors ADR-0156's network shape), rejecting ambiguous multi-container contention rather than picking arbitrarily. Phase D's own live cgroup-program update was empirically tested with a real standalone probe before this document was finalized -- the result simplified the design: no `BPF_F_REPLACE`/`ALLOW_MULTI` needed, a plain re-`ATTACH` (this project's own existing, unmodified `container_dev_bpf_attach()` call shape) already atomically replaces a live program, confirmed against 2,000,000 concurrent access attempts spanning the swap. Every open question resolved directly with the user; implemented, all four phases together (task #884) | Accepted |
| [0162](0162-project-rename-and-its-blast-radius.md) | What a project rename actually costs here: a Secure Boot certificate's own subject, on-disk state paths, the default instance FQDN, binary names, and the image/package names inside every installed host's database. Written for an earlier rename; kept for the blast radius, which outlived the names | Superseded by ADR-0200 |
| [0163](0163-boot-time-persisted-state-never-fatal.md) | A malformed persisted state file must never be fatal to boot -- confirmed live (a bad restore wrote an empty `site_config.json`, which panicked real-PID-1 `cixd` on 192.168.15.95). `siteconfig_load()` now falls back to defaults instead of erroring; a new `boot_subsystem_init()` gate covers all ~20 persisted-JSON-state loaders in the real `--init-mode` boot sequence the same way, while leaving dev/test's fail-fast behavior untouched | Accepted |
| [0164](0164-cli-prompt-fqdn-and-whoami.md) | `cixctl`'s interactive shell prompt becomes `host.site.tld` with `>`/`#` for auth state, backed by a new, deliberately non-mutating `GET /whoami` (a mutating check would consume a single-use session token just by looking at it). Also fixes a real bug the verification work found: mid-session `login`/`logout` updated the token file but never the live client's own in-memory token, so the current session silently stayed unauthenticated until restart | Accepted |
| [0165](0165-pkgbuild-sandbox-resource-limits.md) | Real cgroup memory/CPU limits on the pkgbuild sandbox -- found missing the hard way (a burst of concurrent `pkg install`s hung `cixd` entirely on 192.168.15.95, no confirmed root cause either this time or the prior Part 144 incident). Extends `pkg-build-config` with `memory_max`/`cpu_max` (default 2GiB / one full CPU), reusing the identical `struct cgroup_limits` mechanism every regular container already has; PUT becomes a genuine partial update | Accepted |
| [0166](0166-cgroup-cpu-max-needs-cfs-bandwidth.md) | `cpu.max` needs `CONFIG_CFS_BANDWIDTH`, not just `CONFIG_CGROUP_SCHED`/`CONFIG_FAIR_GROUP_SCHED` -- the real root cause of ADR-0165's own first production regression on 192.168.15.95, found via a precise parent-side last-error-step diagnostic added specifically for this investigation. Kernel config fixed and committed; the actual rebuild is deferred (blocked on a separate, real TCC-vs-binutils compiler issue) | Accepted |
| [0167](0167-container-env-field.md) | `POST /v1/containers` gains a real `env` field, an object of up to 32 `KEY`/`VALUE` pairs rendered into `envp`'s existing execve()-ready shape (no redundant struct added to `container_spec`) -- deliberately kept separate from `files[]`'s own `{{SECRET:KEY}}` recipe-apply substitution (ADR-0151), the same real-runtime-value-vs-build-time-text-fill-in split Docker itself draws | Accepted |
| [0168](0168-container-capability-bounding-set-drop.md) | Issue #29: every container's capability bounding set is trimmed to a default-safe deny-list via `prctl(PR_CAPBSET_DROP)` plus `PR_SET_NO_NEW_PRIVS`, right before `execve()`. `CAP_SYS_MODULE` was the driver -- modules are not namespaced, so any container's root could load code into the shared host kernel with no exploit required at all. A deny-list, not an allow-list, because an allow-list needs an exhaustive per-service audit before it can ship anything; `cap_add` names explicit exceptions (e.g. `CAP_SYS_TIME` for chrony). No `capset()`/libcap needed -- the kernel's legacy-root-exec rule recomputes each exec's sets from the bounding set, verified with a standalone syscall probe rather than trusted from the man page, which is why the drop survives a whole process tree. Does NOT close #29: root in a container is still real host UID 0, and full user namespaces remain open | Accepted |
| [0169](0169-swap-disk-placement.md) | Issue #28: a new `swap` diskrole (same singleton-placement shape state/rebuildable/log-storage already have) + `POST /v1/system/swap`'s own optional `disk` field, reusing `storageplacement.h`'s existing pointer. Deliberately no live-migration job -- swap content is transient/discardable. Two real bugs found and fixed during live verification on 192.168.15.95's real `sda` scratch disk: an ordering bug (repoint before the already-enabled check) and two 409 error messages that didn't mention swap | Accepted |
| [0170](0170-tcc-cgo-tier3-exception.md) | Issue #31's conclusive finding: TCC categorically cannot build cgo-based Go packages -- it aborts its entire compilation unit after the first error anywhere in the file, which breaks `cmd/cgo`'s own batched type-probing mechanism structurally (confirmed against the Go standard library's own `os/user` package, not just glauth/gitea's code). Three earlier, genuinely fixable gaps found along the way (`-Qunused-arguments`, C99 `_Complex`, missing `__atomic_*`/`__sync_*` builtins) are documented but moot given the real wall. glauth/gitea join the Tier-3 TCC exception list; `CC` pinned explicitly to `/usr/bin/gcc` | Accepted |
| [0171](0171-http-https-default-enabled.md) | `https_enabled` now defaults to `true` on a fresh install (ports 80/443 were already the right defaults), matching 192.168.15.95's own real, already-configured state -- a pre-PKI-bootstrap install just has HTTPS silently not come up at boot, non-fatal. A real regression found during verification: `handle_daemon_config_put()` used to hard-500 on ANY unrelated daemon-config change (port, management_network, bind_ip) if HTTPS couldn't start, once it became the ambient default rather than an explicit ask -- fixed by gating the hard-vs-soft-fail decision on whether the request itself explicitly asked for `https_enabled`, caught live by `test_daemon_bind_ip` | Accepted |
| [0172](0172-hostapd-openwrt-patches-and-perl-tcc-fixes.md) | Issue #25: hostapd rebuilt on real upstream 2.11 + 24 curated patches (23 individually read from OpenWrt's own series, 1 real CVE fix from Debian) + vendored libnl-tiny, replacing task #845's plain-upstream recipe. Verified through the real `cixd` pkgbuild pipeline, not just manually -- which surfaced two real, previously-unverified TCC gaps in `perl.recipe` (a parenthesized string-literal array initializer TCC's stricter parser rejects; a `-E`/`-Wl,-E` dynamic-symbol-export flag TCC only recognizes as `-rdynamic`, the second one genuinely load-bearing, confirmed by XS modules failing at runtime when the flag was naively stripped instead of translated) and a real CWD-leak bug in this same hostapd recipe's own `pkg_install()` | Accepted |
| [0173](0173-disk-unmount-endpoint.md) | `POST /disks/{name}/unmount`, a real synchronous `umount2(2)` with no reformat -- closes a genuine gap found chasing issue #34 (a role-removed-but-still-mounted disk had no way to actually be let go of). Deliberately the opposite precondition direction from `format` (works on a role-less disk); shares its two existing data-safety checks verbatim | Accepted |
| [0174](0174-overlay-workdir-project-quota-fix.md) | Issue #34's real root cause, found after two real, properly-tested-and-ruled-out false starts (an `sda` mount correlation; a `redirect_dir=on` mount option that fixed the symptom but broke lowerdir visibility): `overlay_create()` only ever tagged `upperdir` with the container's ext4 project-quota id, never `workdir` -- overlayfs's own internal copy-up-then-rename from workdir into upperdir is exactly the operation ext4's project-quota implementation refuses with `EXDEV` across a tagging mismatch. Confirmed via a live A/B test before shipping; hits essentially every new production container, since a disk quota is required by default | Accepted |
| [0175](0175-pkg-build-keep-on-failure.md) | `keep_on_failure` (issue #35): an opt-in flag on `pkg install`/`pkg hostbuild`/`kmod-build` that preserves a failed build's own exited container instead of tearing it down, addressable afterward via the already-existing `GET .../files` (ADR-0055) and an ordinary `DELETE` -- built to unblock real debugging of issue #32's own gcc bootstrap crash; a `gdb.recipe` was considered and ruled out first (modern GDB is 100% C++, TCC can't build it, and building it with real g++ is circular with issue #32 itself) | Accepted |
| [0176](0176-pkg-recipe-changelog-field.md) | `pkg_changelog=` (issue #44): a third optional, single-line recipe field alongside `pkg_depends=`/`pkg_artifact_sha256=`, shown in a new package-detail "Versions" tab -- deliberately single-line so a real changelog can't sprawl past what was actually asked for ("short"); never backfilled onto the ~80 existing recipes, adopted incrementally going forward. Replaces the Recipes list's old inline "N versions" expand toggle (issue #19) with one browsing surface per concern, matching the pattern the Image detail page's own Versions tab already established | Accepted |
| [0177](0177-pkg-build-resume.md) | `pkg resume` (issue #46): a new `POST /v1/pkg/resume` continues a `keep_on_failure`-preserved build container (ADR-0175) in place under a (optionally newly-fixed) recipe version, reusing its already-extracted source tree and only resetting the previous attempt's partial install output -- no fetch subprocess, no `extract_tarball()`, built to eliminate the real cost of restarting a multi-hour gcc bootstrap (issue #32) from scratch after every one-line recipe fixup. A real use-after-free found and fixed via the new test itself, not by inspection | Accepted |
| [0178](0178-pkg-extra-source-url-query-string-basename.md) | `url_basename()` didn't strip a URL's query string -- found live the first time this project's git-raw-file `pkg_source` mechanism (`?ref=<commit>`) was ever actually exercised end-to-end (`kernel/6.18.40-3`'s own hostbuild), staging `qemu-part1.config?ref=<40 hex chars>` instead of plain `qemu-part1.config` and breaking every recipe's own reference to the plain filename. Fixed with a new regression test verified to actually catch the bug (temporarily reverted the fix, confirmed the new test alone fails, restored it) | Accepted |
| [0179](0179-user-namespaces-by-default-subordinate-id-allocation.md) | Design for issue #29's full user-namespace support: `CLONE_NEWUSER` by default, subordinate UID/GID ranges keyed to `struct ldap_user.uidnumber` (a field that already exists and is already resilient to LDAP being unreachable, via `hostauth_login()`'s own already-existing local-record fallback) -- no glauth schema change needed (checked directly: glauth is a fixed-struct TOML emulation, not an extensible directory, ruling out a FreeIPA-style `ipaSubordinateId` LDAP-attribute approach). 100% API-driven, zero new create-time fields -- container ownership is always the authenticated caller. Not yet implemented; id-mapped-mount feasibility on this project's own overlayfs+ext4 stack is the one genuinely open question | Proposed |
| [0180](0180-async-container-teardown.md) | Teardown of a *running* container (stop and DELETE) becomes asynchronous: the handler writes all durable intent synchronously (service-ownership forgets + `containerdef_remove()`/`set_stopped()`), sends SIGKILL without waiting via `registry_begin_kill()`, and completes the reap/slot-release/disk-cleanup on the entry's own pidfd epoll watch -- the same machinery that already detects crashes. Fixes two real production control-plane freezes caused by the old synchronous unbounded `waitid()` inside the single epoll loop (a task stuck in D-state only ever *pends* SIGKILL). DELETE returns 204, stop returns 200 `"stopping"`, immediately | Accepted |
| [0181](0181-persist-all-containers-restart-decoupled-from-existence.md) | Every container is persisted at create time; `restart` governs only auto-restart, never existence -- decoupling "does it still exist after a stop/reboot" from "does it come back on its own". Supersedes only ADR-0027's `restart:"no"`-is-ephemeral decision (the rest of 0027 stands): `stop` now always keeps a container (reappears `"stopped"`, only `DELETE` removes it -- "stop is stop, delete is delete"), `restart:"no"` means "persisted but never auto-restarted" (not autostarted at boot, crash-restarted, or rolling-followed), and a self-exited container is retained as `"exited"` with its real exit code (Docker `ps -a` semantics, name stays reserved). Closes the reported surprise where `stop` on a default container silently destroyed it | Accepted |
| [0182](0182-registered-server-health-probing.md) | Health probing for every registered LDAP/DNS/NTP/syslog server, as ONE primitive rather than four parallel ones -- prompted by #80, where a registered LDAP pair was running and listening but could not resolve anything under the configured base DN, and no surface anywhere reported a server as bad. Probe is a non-blocking connect on the daemon's own reactor (a blocking one would stall the single event loop -- the ADR-0180 failure); it reports what it actually did (`tcp:PORT` = real service check vs `process` = container merely running, the honest weaker check for the UDP services). One success restores health, several consecutive failures remove it; drain is persisted operator intent while observed health never is. Not-in-service servers are withheld from generated client config, bounded by two rules so filtering can never itself cause an outage | Accepted |
| [0183](0183-persistent-volumes.md) | Persistent volumes -- named storage whose lifetime is independent of any container, so data survives the recreates that `follow_rolling`, recipe edits and package installs perform routinely. Raised by an operator asking whether a `~/.bashrc` on the jump box would survive (it would not; that box was recreated ~12 times in one session). Container DELETE never removes volumes, so volume delete is explicit and refused while any container DEFINITION references it; placement reuses the existing disk-role mechanism rather than inventing a second storage story; the bind mount happens child-side before pivot_root; a failed mount or a failed read-only remount kills the container rather than silently running without persistence or with a false guarantee | Accepted |
| [0184](0184-dashboard-navigation-one-vocabulary.md) | The dashboard's header menu bar and left tree share one vocabulary and divide one job: the menu bar's topics ARE the tree's top-level names in the tree's own order (Containers/Networks/Disks/Services/System), with sub-headings matching that branch's children -- the tree is where you go to look at something, the menu bar is where you go to make one, so whatever branch a thing lives under is the menu that creates it. Prompted by two menu topics ("Hardware", "User / Group") that were not a word anywhere in the tree, leaving the same subject with two names depending on which half of the screen you looked at. Alignment is physical too (logo cell = tree column width from the same CSS variable, first topic at the content pane's own inset); double-clicking a tree row now opens/closes it. Client-side only -- every action already had an endpoint | Accepted |
| [0185](0185-menu-submenus-and-tabs-as-addresses.md) | Menu branches with children (Services, System) get real hover submenus, one per child of that branch -- the menu bar mirrors exactly two levels of the tree and stops there; supersedes only ADR-0184's "one level with headings" call (pointing at three things beats reading past three headings). Monitoring collapses into one tabbed page, removing the tree's last folder. And every tab on a collapsed page is an ADDRESS: clicking one navigates, so its route handler runs and the panel actually has data -- fixing a real bug where `Host > Routes` sat on "Loading…" indefinitely because the panel was revealed but nothing had asked the daemon for routes | Accepted |
| [0186](0186-header-chrome-menu-bar-is-not-a-grid-column.md) | The header is ordinary chrome, not a continuation of the layout grid: the logo cell is no longer stretched to the tree column's width (tried on a real screen -- a large empty block with the menu pushed a quarter of the way across the header), and the theme toggle drops its solid-accent button styling for a plain icon in the page's own text color. Supersedes only ADR-0184's pixel-alignment decision; the vocabulary rule that ADR exists for is untouched, and `--tree-width` stays as the one named place for the column's width | Accepted |
| [0187](0187-control-plane-reservation.md) | Bound everything that is NOT the control plane rather than prioritising it: every container and build is a leaf under one `cix-workload` cgroup capped at the machine minus a configured reservation (builds nest inside it, so the build budget cannot sit beside the workload budget and add up to more than the machine). Expressed as what the control plane KEEPS, derived from live host totals on every apply, so the same config means the same thing on a 2-CPU VM and on real hardware. On by default (10%, 512 MiB), capped at 50% so it stays a safety margin. Closes the third and structurally sound part of issue #86, after nice -20/oom_score_adj -1000 (priority) and #85 (the specific trigger). Rejected SCHED_FIFO: a control plane that can never be preempted fails harder than one that can be starved | Accepted |
| [0188](0188-per-package-rolling-policy.md) | Per-package rolling policy -- `highest` (dpkg-style, unchanged default), `newest` (most recently published recipe, using the write-once mtime ADR-0107's immutability makes a real publication timestamp), or `pinned` (a real hold). Prompted by the Part 201 toolchain work publishing gcc 4.7.4 and 6.4.0 AFTER 16.2.0, so highest and newest diverge and neither is what a bootstrap chain wants. Applied in the single resolver every implicit resolution already goes through, so update-all and follow_rolling need no knowledge of policies and a hold cannot be forgotten by a caller. A pin with no version, and a pin to an unpublished version, both fail loudly rather than drifting | Accepted |
| [0189](0189-control-plane-stall-watchdog.md) | A forked watchdog PROCESS records when the event loop stops going round -- because the loop that would report its own silence is the one that is silent, which is why a real multi-minute outage on the production host left nothing in the log store at all. A process rather than a signal handler specifically so it can read /proc/<pid>/wchan and the process state, the two most useful facts about a wedge and the ones unavailable from inside it; the loop now wakes once a second so idle is distinguishable from wedged; records are JSON lines that outlive the daemon and the reboot. Deliberately does NOT restart the daemon yet -- that threshold would be a guess until these records say what actually stalls | Accepted |
| [0190](0190-install-leaves-the-disk-mostly-unallocated.md) | A fresh install claims what it needs and leaves the rest of the disk unallocated: the data partition is the smaller of a 16 GiB cap and half of what remains, instead of "the rest of the disk". Raised by the operator before the first bare-metal install -- an installer that consumes everything makes its own guess irreversible, since undoing it means shrinking a filesystem holding live state, while growing into free space later is already supported (#94). Checked rather than assumed while scoping: the config partition really is used (it carries net.conf, read on first boot), and no disk role was ever auto-assigned. Refines ADR-0018's layout; its decision stands | Accepted |
| [0191](0191-per-container-ldap-login-restriction.md) | Which users may log into a container is a per-container statement, enforced by nslcd: `ldap_allow_groups` renders a `pam_authz_search` into the staged `/etc/nslcd.conf`, so the restriction holds without Cix running. Group membership chosen over the traditional `host` attribute from live evidence -- glauth's config backend exposes no custom attributes, but already emits `memberOf` for primary and secondary groups both. Absence means no filter rather than a permissive one; names are refused, never escaped; an unknown group is refused at creation. Extends ADR-0144 | Accepted |
| [0192](0192-cix-identifier-prefix.md) | One identifier prefix, and it is the project's own name: a legacy prefix inherited from an earlier name becomes `cix_`/`CIX_` across 3,601 occurrences. Adopting it introduces no convention -- it finishes applying the one already used by everything named since. `tc_` rejected for a project-specific reason: it reads as Linux traffic control in a codebase shipping its own data plane, and as Tiny C in one whose only compiler is TCC | Accepted |
| [0193](0193-kernel-release-channel-policy.md) | Which kernel line a box tracks is a per-box channel (kernel.org's own monikers), resolved from kernel.org's own releases.json with no mirror of that data in this repo. It reports rather than acts: selecting a channel never rewrites the recipe pin and never fetches. Null is a real answer (a box that never asked is not a box that is up to date); `longterm` resolves within the line the box already runs, since kernel.org lists six at once. Deliberately stops short of auto-generating a pin, which would turn `pkg_sha256` from operator-verified into trust-on-first-use | Accepted |
| [0194](0194-network-ports-from-the-kernel.md) | A network's ports are read from the kernel (`/sys/class/net/<bridge>/brif`), not from this daemon's registry, which can only ever report what it believes it did. The registry annotates; a port nothing can account for is reported as `unattributed` rather than dropped, which is the diagnostic the whole design exists for. No port numbers (ports come and go with containers, so none would be stable); counters named from the port's own side, since the direction is the difference between a useful number and a misleading one | Accepted |
| [0195](0195-per-container-swap-limit.md) | A per-container swap limit (`memory_swap_max` -> cgroup v2 `memory.swap.max`), where **0 is a real setting** -- may not swap at all -- so it cannot use the 0-means-unset convention every other limit here uses: `-1` in the struct, absent in the API, `-2` in the CLI, and `0` never folded into `null` on read-back. Needed no kernel change: `CONFIG_MEMCG_SWAP` was removed upstream before 6.1 and `memory.swap.max` is unconditional wherever memcg and CONFIG_SWAP are on, checked against the real 6.18 source rather than assumed | Accepted |
| [0196](0196-zswap-owned-by-the-daemon.md) | zswap compiled in, with `CONFIG_ZSWAP_DEFAULT_ON` deliberately unset so the daemon is the only thing that decides whether it is on -- one source of truth for one fact. Three compressors compiled in, since the runtime knob only reaches what the kernel has; configured intent and kernel state reported separately, because the kernel silently ignores what it cannot honour; a failed apply persists nothing. On by default, unlike the swap file, which consumes real disk | Accepted |
| [0197](0197-dhcp-served-by-the-dns-server.md) | DHCP is a **self-contained service**: its own server registry, its own ranges and reservations, every endpoint under `/v1/dhcp`, reading no other service's state -- which is what has to be true for a service to become a loadable plugin. Its pairing with DNS (register the same container for both and leases resolve instantly) is *reported*, not enforced. Redundancy is split scope (each server gets a disjoint slice, computed not configured), because dnsmasq implements no failover protocol: disjoint pools are the only thing stopping two servers handing one address to two machines. A lease is **not** a DNS record: records are durable operator intent, leases are the server's own state, read back from its lease file and never mirrored into the record store. Static reservations are live via SIGHUP; a range change restarts the servers through the same jittered rolling-restart timer, because dnsmasq reads ranges only at startup and a silently-inert setting is the failure mode being avoided | Accepted |
| [0198](0198-build-sandbox-is-a-real-image.md) | The shared build sandbox becomes the ordinary `cix-builder` image, resolved the same way a hostbuild's `--build-image=` already is -- one mechanism, not a flat unversioned directory living beside the image model. Walks into and out of ADR-0155's same-manifest trap: folding a package in changes no manifest, so the naive version hash is identical and the merge is silently discarded -- fixed with an identity chain over the previous version. Existing flat content is migrated in rather than dropped, because a refactor must not smuggle in a content change | Accepted |
| [0199](0199-recipes-declare-their-build-tools.md) | A recipe declares its build tools (`pkg_build_depends=`) and its build container is composed from exactly those packages' own cached output -- no extras to accidentally depend on, nothing missing the build will not name. The composed environment is an image keyed by the hash of the declared set, so it is built once and reused. A declared tool that cannot be provided **fails the build**; there is deliberately no fallback, because a fallback would make the declaration decorative. Supersedes ADR-0198's direction: measurement showed 67% of the shared sandbox was an undeclared Rust/Go toolchain | Accepted |
| [0200](0200-rebrand-to-cix.md) | Rebrand to Cix (`cixd`/`cixctl`, `cix_` prefix, `/var/lib/cix`, `Cix Root CA`). Three owner decisions recorded: `cixctl` over the guidelines' own `cix`; zero occurrences of either previous name anywhere, CHANGELOG and the frozen charter included; and the live host deferred entirely, so the repo rename disturbs nothing in flight. Signing key regenerated (MOK re-enrollment required); `docs/brand/` deliberately untouched | Accepted |
| [0201](0201-artifacts-are-retrievable-and-self-publishing.md) | A built artifact can leave the machine that built it: `GET /pkg/{name}/artifact` streams a hostbuild's output as a tarball, and a builder can auto-push it to a configured cache. Closes the gap where the only way to move a build between hosts was an out-of-band file copy | Accepted |
| [0202](0202-the-esp-is-reachable-over-rest.md) | The ESP becomes a REST resource: `loader.conf` readable and writable, boot entries enumerable and deletable. Prompted by a host that could not complete an A/B update because `default thinc-*` — a glob **pattern**, not a name — kept matching six stale pre-rename entries and outranking the correct new one, with no error possible anywhere. Guards refuse a default matching zero entries and refuse deleting the running slot's own entry | Accepted |
| [0203](0203-dns-forwarders-are-daemon-owned-config.md) | Upstream DNS forwarders become daemon-owned config (`GET/PUT /v1/dns/forwarders`) applied live to every registered server, instead of `--server=` flags baked into each container's argv at creation time. That convention meant changing a forwarder required recreating core infrastructure, two replicas could silently disagree, and nothing could answer "what does this platform resolve through?" at all. Supersedes the forwarder convention that grew up alongside ADR-0076 | Accepted |
| [0204](0204-the-daemon-fetches-its-own-update-image.md) | `POST /system/update` accepts `image_url` and fetches the image itself, with a **mandatory** `image_sha256`. `image_path` assumed a file could already be placed on the box — but an installed host has no shell and no scp target, and the 1 MiB request cap rules out a body upload, so the one endpoint whose job is updating the control plane could not update it. The URL is rewritten into the existing `image_path` flow so slot-writing keeps a single code path | Accepted |
| [0205](0205-workflows-are-endpoints.md) | A multi-step operator workflow is an endpoint, not a sequence driven from a client. `cixctl dns provision` orchestrated four REST calls itself -- a CLI feature with no endpoint behind it, which the mandate forbids and which forces the dashboard to reimplement the same sequence to reach parity. Moved to `POST /v1/dns/provision`; the CLI becomes argument parsing over one call. Required splitting `handle_create()` into a reusable `create_container_persisted()`, so a workflow endpoint creates containers through the same implementation as `POST /containers` rather than a second one | Accepted |
| [0206](0206-configuration-as-a-first-class-document.md) | Configuration becomes a first-class document: `GET /v1/config` returns the canonical structured document, `POST /v1/config` applies one all-or-nothing (`configure replace`, not `configure terminal`), `GET /v1/config/diff` answers "what changed". The document is always DERIVED, never stored as a second copy of state; the **schema generates** the CLI vocabulary and the dashboard rendering, so alignment is structural rather than maintained. Presentation (Cisco-style text, paging) is client-side; secrets are redacted, never rendered; there is deliberately no `startup-config`, since a Cix host persists state continuously. Config mode is an editor over the document, so it costs the core two endpoints | Proposed |
| [0207](0207-btrfs-storage-substrate-userns-by-default.md) | btrfs becomes the sole storage substrate for the platform's writable storage; a container's rootfs is a writable btrfs **snapshot** of its image subvolume (CoW, persistent, thin) rather than an OverlayFS lowerdir+upperdir, and overlay retires completely. This removes the one thing that made id-mapped mounts impossible (overlay's workdir setup needing init-userns privilege), so **user namespaces become the default** — every container a mapped unprivileged uid, opt-out via `userns:false`, the snapshot and its volumes presented through `MOUNT_ATTR_IDMAP`. Supersedes ADR-0079/0080; completes ADR-0179/#29's userns goal; ext4 retired from platform storage after btrfs is proven live on .95. Phased, each phase verified before the next | Accepted; phased |
| [0208](0208-build-image-taxonomy.md) | One job per build image, named for that job. `cix-builder` was simultaneously the operator-facing general build image and `PKG_BUILD_SANDBOX_IMAGE`, the shared sandbox every successful install folded into -- so the image an operator named on a command line was also the union of everything ever built on that host. Split into `cix-builder` (ordinary packages), `kernel-builder` (the kernel's own toolchain), and `iso-builder` (installer media), each composed from what its job declares and nothing else | Accepted |
| [0209](0209-derived-images-and-one-container-mechanism.md) | An image is **derived** from its manifest, materialized while referenced and garbage-collected when not -- so its manifest is true by construction and drift is impossible rather than merely detected. Whole-rootfs image artifacts (ADR-0123's fast path) are dropped: package artifacts become the only published binaries, each checksummed and traceable to a recipe and the Cix host that built it. One mechanism creates every container, build containers included. The baseline is composed from packages instead of copied from the host, leaving `glibc`/`ld.so` explicitly named as the last unclosed link in the bootstrap. Caused directly by #168/#169: `cix-builder` measured 8 GB across 88,798 entries, 93% of it belonging to no package it declared | Accepted |
| [0210](0210-no-privileged-base-image.md) | `base` stops being special. Nothing inherits from it -- every image has its own independent rootfs -- so it was never a foundation; it was `PKG_DEFAULT_IMAGE` plus an undeletable flag, with a name suggesting a hierarchy the system does not have. Its imperative seeding in `cix-install.c` (a C runtime copied in by code rather than declared by a manifest) is removed, since `pkg_seed_image_baseline()` already gives every image the same thing. The **name** survives purely as the default two paths need; the content rule becomes uniform across every image, with no exception left to reason about | Accepted |
| [0211](0211-tcc-msabi-and-atomics-tier3.md) | `gnu-efi` and `python` cannot be built with TCC, proven rather than asserted: gnu-efi needs `__builtin_ms_va_list` for the EFI calling convention (TCC has `__builtin_va_list` only — reduced to a one-line probe), and CPython 3.13's `pyatomic.h` reaches an explicit `#error` because TCC offers neither GCC/clang atomic builtins nor C11 `stdatomic.h`. Both join ADR-0170's Tier-3 exception list, built with **Cix's own** gcc 16.2.0-11 from the artifact cache — which does not weaken the Build Provenance Mandate, since that constrains where and by what something is built, not which Cix compiler. `tcc` is dropped from these recipes' build environments rather than left beside `gcc`, because two compilers in one environment is how a real GCC was once silently picked over TCC. `binutils-dev`/`sbsigntools` follow as dependents, with binutils-dev explicitly marked provisional | Accepted |
| [0212](0212-signing-keys-over-rest.md) | The Secure Boot signing key pair `POST /system/iso` requires reaches a host over REST (`GET`/`PUT`/`DELETE /v1/system/signing-keys`), pasted in as two PEM blocks. Supersedes the *transport* half of ADR-0064, whose "operator populates it out of band" compared the requirement to a file already `scp`'d onto the box — but a real Cix host runs no sshd, exposes no host-side exec and has no console, so that endpoint has never been servable on any installed machine. ADR-0064's security property is preserved exactly: cixd still never generates, fetches or copies the key on its own initiative, and only the public DER `.cer` is ever staged onto an installed target. The key goes in and never comes back (`key_set`/`cert_set` booleans plus the certificate's public identity, the same shape `auth_token_set` already has); the `.cer` is derived from the certificate rather than accepted separately; and the pair is verified to match before anything is written, because a mismatched paste would otherwise surface as an image that signs cleanly and then will not boot | Accepted |
| [0213](0213-scripted-partitioning-is-the-install-default.md) | Scripted partitioning becomes `cix-install`'s default; the interactive `fdisk` session stays behind an explicit `--interactive`, and `--auto-partition` is removed rather than kept as a no-op alias. The default flipped not for convenience but because the interactive path was never the safer one: `cix-install` reads partition *roles* back from GPT partition **names**, so driving `fdisk` by hand means reproducing five exact names in order with the right types — a contract `fdisk`'s interface says nothing about, whose violation surfaces much later as a role that cannot be found. ADR-0190 had already made the scripted layout adaptive and conservative, and a running host partitions the deliberately-unallocated remainder through the REST API, so the usual reason to reach for a manual editor is now answered after install rather than during it. Deleting the interactive path outright was considered and rejected on one fact: the installer media carries no shell, so without `fdisk` a machine the standard layout does not suit could not be partitioned from the ISO at all | Superseded by ADR-0214 |
| [0214](0214-no-interactive-partitioning.md) | There is no interactive partitioning. Supersedes ADR-0213, whose first decision (scripted by default, `--auto-partition` removed) stands and whose second (keep the `fdisk` session behind `--interactive`) was reversed once its two arguments were checked against the code. "The media has no shell, so fdisk is the only escape hatch" has a true premise and a false conclusion: `find_partition_device()` requires five exact GPT names, so the structure is fixed no matter who writes the table and an interactive session could only ever vary four sizes — one of which (`cix-containers`) is already better served by growing into the space ADR-0190 deliberately leaves free, and three of which are platform constants (the control-plane image is ~10 MB against a 160 MiB root slot). "We would lose the QEMU fdisk test that caught a real bug" is circular: it keeps a feature to keep the test of that feature. `fdisk` is no longer packaged, harvested or staged; `--skip-partition` remains for a disk prepared elsewhere | Accepted |
| [0215](0215-our-own-efi-boot-manager.md) | An installed host boots through Cix's **own** EFI boot manager rather than systemd's `sd-boot`. Explicitly does *not* supersede [ADR-0014](0014-squashfs-ab-root-with-native-boot-counting.md): its counted A/B slot mechanism, on-disk layout and Boot Loader Specification entry format are unchanged — only the program that reads them is ours | Accepted |
| [0216](0216-the-glibc-floor-is-closed.md) | The last four files this project did not build — `ld-linux-x86-64.so.2` (both spellings), `libc.so.6`, `libm.so.6`, `libnss_files.so.2` — stop being *copied* off whatever machine runs `cixd` and start arriving as a `glibc` **package**, built on a Cix host with this platform's own toolchain. Closes the caveat ADR-0209 named as its own unfinished business. A composed build environment gets its C library implicitly and verified, so an image's runtime is described by a manifest rather than borrowed by absolute path | Accepted |
| [0217](0217-retiring-libc-dev.md) | `libc-dev` is deleted, and its cache artifacts purged rather than orphaned. It staged another distribution's headers and CRT objects by copying them off the bootstrap machine — its own recipe called that "the last unclosed link in this project's self-hosting" — and it was pinned to glibc **2.36** while the runtime ran **2.44**, so every package compiled against headers eight releases behind the library it linked, with `gcc`'s own `pkg_depends` carrying it into every environment holding a compiler. `glibc` now owns the C headers and CRT objects, `linux-headers` the kernel UAPI. Accepted cost, stated rather than discovered: 354 pre-retirement recipe revisions become unbuildable, since rebuilding 30 never-built target versions to preserve revisions nobody would rebuild is hours of work for insurance a rolling-release catalogue does not need | Accepted |
| [0218](0218-the-api-is-the-daemon-and-decides-what-each-channel-exposes.md) | Two different guarantees, deliberately. **The daemon admits no wrong state**: every `/v1` operation in `openapi.yaml` gets one generated dispatch entry, unconditionally — an operation with no handler is a **link error**, a handler no operation names is unreachable, and there is nothing to declare or check. **The channels admit one**, so `x-cix-expose` says which of them must expose a capability — the API decides, in one place, instead of each channel deciding and drifting. Exposure is validated in three layers whose trick is inverting the question: enumerating which URLs a channel builds proved unreliable (three attempts, three answers, one supporting the false claim that the dashboard cannot stop a container), but asserting it builds **none** outside generated entry points needs no understanding of the code at all. Layers 1–2 prove a symbol exists; only layer 3 proves it is reachable — the #192 lesson that detecting is not preventing. Only routing is generated; commands, flags and page design stay hand-written | Accepted |
| [0219](0219-versioning-and-release-tags.md) | Ordinary `vMAJOR.MINOR.PATCH` versioning, with `-rcN` strictly pre-release. Written because the convention never was: `v2.0`/`v2.1` used no candidates at all, then 34 `v2.2.0-rcN` tags appeared and did not stop when `v2.2.0` shipped -- `rc31`-`rc35` postdate the release they claim to precede. It stayed invisible because `pkg_version_cmp()` ranks `2.2.0-rc35 > 2.2.0`, the reverse of SemVer, so every internal decision came out right while the tags said something no external reader would agree with. The comparator keeps its behaviour deliberately (changing it re-ranks every artifact and installed package on every host), and the disagreement is recorded rather than left to be rediscovered | Accepted |
| [0220](0220-a-separate-release-signing-key.md) | A second, Ed25519, operator-supplied key signs published installer ISOs. #197 specified using the existing platform key *and* Ed25519, which cannot both hold: that key is the **Secure Boot** key, handed to `sbsign`, and UEFI mandates RSA while minisign is Ed25519-only. The separation is right regardless of the algorithm clash -- sharing one key means whoever can sign a download can sign a bootloader. Signs the raw ISO in minisign's legacy `Ed` mode (OpenSSL's Ed25519 is PureEdDSA, so no BLAKE2b is needed), derives the key id from `SHA-256(pubkey)` so the public key and every signature agree by construction, and emits the global signature over `signature ‖ trusted comment` -- the part naive implementations omit and stock minisign rejects. Verification is deliberately upstream minisign, not ours: this is the artifact that gets booted | Accepted |
| [0221](0221-build-environments-are-reclaimed-by-last-use.md) | Issue #112: composed build environments are reclaimed by LAST USE, not by reachability over the recipe catalogue. The reframing: composition is cheap and deterministic, so deleting one that turns out to be wanted costs a *recomposition*, not a failure -- a cost question with automatic recovery, not a correctness question. Reachability would be expensive, still approximate, and buys precision with no value. The one hard rule is never touching an environment an in-flight build holds. Deliberately not folded into cache pruning: prune the cache and a build gets slower, prune an environment and it gets recomposed -- one number covering both would hide which happened | Accepted |
| [0222](0222-a-declared-capability-loss-beats-a-foreign-toolchain.md) | A recipe may ship less than upstream provides when a real, identified defect prevents building it -- but only if the loss is *declared*: named in the recipe header and changelog in operator terms, linked to its issue, stating what is KEPT as well as what is lost, asserted in both directions so neither half can be wrong silently, and reverted when the defect is fixed. Chosen over adding the package to the Tier-3 TCC exception list, which looks cheaper and is not: an exception is quiet (the artifact says nothing about which compiler built it) and would become the ordinary way past any TCC gap. Deliberately narrower than "ship whatever builds" -- a feature lost to a failed configure probe is #113, not this | Accepted |
| [0223](0223-the-compiler-is-a-pinned-upstream-snapshot.md) | The compiler is a pinned upstream snapshot, not a patched release | Accepted |
| [0224](0224-the-toolchain-tenet.md) | The Toolchain Tenet — TCC by right, gcc by evidence | Accepted |
| [0225](0225-build-images-are-composed-from-packages.md) | Build images are composed from packages, not copied from a host | Accepted |
| [0226](0226-gcc-is-an-ordinary-choice-for-third-party-packages.md) | gcc is an ordinary choice for a third-party package, not a debt | Accepted |
| [0227](0227-responses-are-buffered-and-drained-never-blocking.md) | Responses are buffered against their connection and drained on EPOLLOUT, never written blocking | Accepted |
| [0228](0228-build-environment-composition-runs-in-a-forked-child.md) | Build-environment composition runs in a forked child, and the install resumes | Accepted |
| [0229](0229-installer-media-carries-a-package-seed.md) | Installer media carries a package seed, so a fresh box can name the network | Accepted |
| [0230](0230-the-five-lifecycle-domains.md) | The platform has five lifecycle domains, named in the CLI and the docs, not in URLs | Accepted |
| [0231](0231-attached-disks-do-not-mount-inside-the-data-directory.md) | Attached disks mount at /mnt/cix on a tmpfs, not inside the data directory | Accepted |
| [0232](0232-state-lives-on-the-config-partition.md) | State lives on the config partition; the state-storage role is retired | Accepted |
| [0233](0233-setgroups-stays-permitted-in-a-root-created-userns.md) | setgroups stays permitted in a root-created user namespace | Accepted |
| [0234](0234-snapshot-containers-migrate-across-disks.md) | Snapshot containers migrate across disks, at the cost of extent sharing | Accepted |
| [0235](0235-build-capacity-is-derived-from-state.md) | Build capacity is derived from package state, not from a field someone has to clear | Accepted |
| [0236](0236-a-disk-is-identified-by-its-filesystem-uuid.md) | A disk is identified by its filesystem UUID, not by its kernel name | Accepted |
| [0237](0237-a-missing-placement-disk-degrades-rather-than-refusing-to-boot.md) | A missing placement disk degrades the daemon, it does not stop it | Accepted |
| [0238](0238-containers-have-no-outbound-internet-access.md) | Containers have no outbound internet access, deliberately | Accepted |
| [0239](0239-a-container-facing-filesystem-is-owned-by-the-containers-root.md) | A filesystem mounted for a container is owned by the container's own root | Accepted |
| [0240](0240-a-container-declares-its-consoles.md) | A container declares its consoles, and declaring none means it has none | Accepted |
| [0241](0241-the-library-layout-is-one-directory.md) | The library layout is a decision, and it collapses to one directory | Accepted |
| [0242](0242-a-console-is-a-sized-terminal-of-a-declared-type.md) | A console is a sized terminal of a declared type | Accepted |
| [0243](0243-the-dashboard-terminal-is-a-real-vt.md) | The dashboard terminal is a real VT, written here | Accepted |
| [0244](0244-the-control-plane-is-protected-its-children-are-not.md) | The control plane is protected, the workloads it spawns are not | Accepted |
| [0245](0245-the-exec-command-is-a-query-parameter.md) | The exec command is a query parameter, and the header is retired | Accepted |
| [0246](0246-the-control-plane-is-one-process-doing-five-jobs.md) | The control plane is one process doing five jobs, and it needs to be two | Superseded by ADR-0247 |
| [0247](0247-the-reactor-does-not-block-and-that-is-the-defence.md) | The reactor does not block, and that is the defence | Accepted |
| [0248](0248-gcc-is-a-linter-never-a-producer.md) | gcc is a linter, never a producer | Accepted |
| [0249](0249-main-c-is-divided-by-what-a-handler-touches.md) | main.c is divided by what a handler touches, not by what it is named | Accepted |
| [0250](0250-a-build-that-cannot-find-a-tool-is-a-failed-build.md) | A build that cannot find a tool is a failed build, whatever it exits with | Accepted |
| [0251](0251-a-package-artifact-carries-what-the-platform-runs.md) | A package artifact carries what the platform runs, and nothing else | Accepted |
| [0252](0252-an-image-recipe-is-authoritative.md) | An image recipe is authoritative; the image is derived from it | Accepted |
| [0253](0253-a-build-output-tree-is-created-fresh.md) | A build output tree is created fresh, never inherited | Accepted |
| [0254](0254-upstream-checksums-are-verified-not-computed.md) | An upstream checksum is verified, never computed here | Accepted |
| [0255](0255-a-recipe-is-a-rule-not-a-version.md) | A recipe is a rule, not a version | Accepted |
| [0256](0256-the-pipeline-is-the-model.md) | The pipeline is the model | Accepted |
| [0257](0257-one-scheduler-structured-schedules.md) | One scheduler, and a schedule is structured, not a string | Accepted |
| [0258](0258-storage-not-disks.md) | The resource is Storage; a disk is a device in it | Accepted |
| [0259](0259-an-interface-may-be-named.md) | An attachment's interface may be named, and defaults to the position it always had | Accepted |
| [0260](0260-a-container-declares-services-not-a-command.md) | A container declares the services it runs, not a single command | Accepted |
| [0261](0261-reaching-inside-a-container.md) | Reaching inside a container: what a console is, what exec is, and what you find there | Accepted |
| [0262](0262-a-container-sees-its-own-limits.md) | A container sees its own limits, not the host's: one host-side FUSE server for a virtualised /proc | Proposed |
| [0263](0263-firmware-is-an-image-the-host-root-is-assembled-from.md) | Device firmware is an ordinary image, and the control-plane root is assembled from its `lib/firmware` | Accepted |
| [0264](0264-a-container-may-bridge-its-own-interfaces.md) | A network attachment may be a port of a bridge inside the container, so an AP's radio and wired segment meet | Accepted |
| [0265](0265-a-radio-moves-as-a-phy-not-as-an-interface.md) | A radio moves as a whole PHY over nl80211, not as an interface over rtnetlink, so it can enter a container | Accepted |
| [0266](0266-the-module-tools-are-an-image-the-module-tree-is-the-kernels.md) | The module tools are an image and the module tree is the kernel's own artifact, so a kernel module can load at all | Accepted |
| [0267](0267-a-volumes-size-comes-from-the-kernel-when-the-kernel-is-counting.md) | A volume's size comes from the kernel when the kernel is already counting, and the answer says which source it came from | Accepted |
| [0268](0268-gating-is-visible-and-its-preconditions-are-protected.md) | Write authentication is visible, and the five operations that could silently turn it off are refused | Accepted |
| [0269](0269-one-pipeline-model-for-four-kinds.md) | One pipeline model for packages, images, deployments and the host -- four kinds walking subsets of one stage vocabulary | Accepted |
| [0270](0270-a-deployment-waits-for-its-image-rather-than-being-refused.md) | A deployment whose image is not built waits for it and is created when it lands, rather than being refused | Accepted |
| [0271](0271-an-action-is-attributable-to-a-person.md) | The caller's username is resolved once at dispatch and named in the audit trail, refusals included | Accepted |
| [0272](0272-a-pipeline-run-is-a-log-entry-not-join-state.md) | A pipeline run is an append-only event with its own retention, never read to compute a position | Accepted |
| [0273](0273-a-gate-holds-automation-where-a-change-escapes-its-blast-radius.md) | Three gates hold automation where a change escapes its blast radius; a held item is `blocked`, not a new status | Accepted |
| [0274](0274-the-platform-says-what-it-is.md) | The platform ships `/etc/os-release` with `ID=cix`, rendered once and staged into both the host root and every image | Accepted |
| [0275](0275-a-stage-has-phases.md) | A stage may have phases — steps inside one stage, same status axis, never reported without their stage | Accepted |
| [0276](0276-a-link-is-a-declaration.md) | A package must declare every shared library it links, checked at install against the declared dependency closure rather than against what happens to be present in the target image — the presence check is the one that would have passed the package that caused this | Accepted |
| [0277](0277-a-container-rootfs-is-derived-from-its-image.md) | A container's rootfs is derived from its image version: when the pin moves the tree is rebuilt, so in-container writes do not survive an image version change and `follow_rolling` applies for the first time | Accepted |
| [0278](0278-the-reactor-forks-work-it-cannot-afford-to-wait-for.md) | Work that can take unbounded time runs in a forked helper and the reactor learns it finished from a pidfd; a process rather than a thread, and filesystem-effecting work only | Accepted |
| [0279](0279-an-artifact-carries-its-own-approval.md) | An artifact is approved by a minisign signature published beside it, binding its recipe revision in the trusted comment; the recipe's checksum stays as an independent pin, and the trust anchor is `docs/keys/` adopted at sync | Accepted |
| [0280](0280-a-certificates-identity-and-its-lifetime-are-separate-concerns.md) | A certificate's identity (name and SANs) is separate from its lifetime (`owner_container`), so a container can be given a durable identity it does not own — `pki_cert` delivers one, because TLS verifies the CA while SSH pins the exact key | Accepted |
| [0281](0281-the-ca-leaves-the-box-encrypted-or-it-is-lost.md) | The whole PKI store can be exported off the box encrypted under an operator passphrase and imported back, narrowly superseding "the CA key never leaves over the API"; and the installer keeps a CA it finds rather than formatting over it | Accepted |
| [0282](0282-glauths-listeners-are-configuration.md) | Which listeners a glauth serves is API configuration rendered into its own config file, not literal recipe TOML — extending ADR-0148's baseDN ownership; `client_tls_port` goes away because the port clients get is the server's own, and a `listeners_managed` flag keeps the first render off a live install | Accepted |
| [0283](0283-the-management-address-is-changeable-on-a-running-box.md) | A booted box's management interface, address, prefix and gateway are changeable through `PUT /system/management-network`, applied live and then persisted to `net.conf` — which the daemon now writes, so the format moved into one module both it and `cix-install` link | Accepted |
| [0284](0284-cixd-always-binds-loopback.md) | `cixd` always binds `127.0.0.1` and the box's chosen address is a *second* listener, never an alternative — the console shell and the watchdog talk to loopback, so a management address that cannot be bound costs remote reachability and nothing else |
| [0285](0285-the-reactor-cannot-reach-a-blocking-teardown-and-the-watchdog-can-act.md) | `registry_remove()` can no longer kill a running container, so the reactor cannot reach the unbounded `waitid()` that froze a live box — three callers move to ADR-0180's async teardown behind new `RESTART`/`MIGRATE` kinds; and `stallwatch` becomes `cix-watchdog`, every threshold and its escalation action configurable over `/system/watchdog`, with teeth that diagnose from `/proc/<pid>/syscall` and then reboot | Accepted |
| [0286](0286-a-container-sees-its-own-disk.md) | A container sees its own disk: `/proc/partitions` rendered from its own mount table through `cix-procfuse`, and a 34-line btrfs patch making `statfs()` report the subvolume's qgroup limit — chosen over a loop-backed image (which would destroy O(1) snapshot seeding) and over a FUSE passthrough root (which would mean owning a filesystem in the path of every file operation) | Accepted |
| [0287](0287-the-management-address-is-the-single-truth.md) | The management address is the single truth; the network that carries it and its `management` flag are derived from it, never stored — collapsing the management-network / `is_management` / `bind_ip` trio to one input (supersedes ADR-0068, amends ADR-0058/0067) | Accepted |
| [0288](0288-the-data-directory-partition-grows-online.md) | The data-directory partition (`/var/lib/cix`, never unmountable) grows online, in place, on its live mountpoint — a mounted btrfs is grown where it is via `btrfs filesystem resize max`, and every resize's table grow moves to the append path's `--no-tell-kernel` + `BLKPG_RESIZE_PARTITION` so it works on a busy disk (closes the grow direction of #249, completes #94) | Accepted |
| [0289](0289-a-hostbuild-package-is-consumed-from-the-cache-like-any-other.md) | A hostbuild package (`cix`, `kernel`, `isotools`) is consumed from the artifact cache like any other — the remote signed-artifact tier was never hostbuild-gated, so a signed hostbuild artifact is fetched rather than rebuilt (trust-anchored on the ADR-0279 release-key signature), and the local-cache read no longer excludes a hostbuild (amends ADR-0122, whose exclusion was about the harvested output, not the read) | Accepted |
| [0290](0290-the-boot-manager-is-updated-in-place-with-no-fallback-of-its-own.md) | A new `GET`/`POST /system/boot-manager` updates the ESP's `\EFI\BOOT\BOOTX64.EFI` on an already-installed host — the one file both A/B slots share with no per-slot fallback, so it is written staged-then-renamed with a same-directory backup, validated only by a shallow PE-magic/size check, and never folded into `update` | Accepted |
| [0291](0291-the-control-plane-root-is-an-image.md) | Design proposal: the control-plane root becomes a real image (`cix-boot`), manifest-declared like every other image, replacing `mkbootroot.c`'s five-mechanism hand-stitching (harvest paths, two image sweeps, a hardcoded tool table) that produced two real incidents (ADR-0029 no-caller, #347); names the unresolved `pkg_depends`/elfcheck/hostbuild tension (#465) this design runs into | Proposed |
| [0292](0292-configuration-applies-section-by-section.md) | Configuration applies section by section: the `ConfigDocument` schema declares per section whether one setter replaces it (`replace`, 11 sections, applied) or whether applying means reconciling individual live resources (`reconcile`, 22 sections, diffed only and refusing the whole request rather than half-applying it); adds `POST /config` + `POST /config/diff`, one plan computed by one code path, and records that the document mixes configuration with running state | Accepted |
| [0293](0293-an-unbounded-measurement-is-served-from-a-cache.md) | An unbounded measurement (`overlay_upperdir_size()`, an O(files) `nftw()` walk) is measured in a `helper_run()` child and served from a cache with an `upper_measured_at` timestamp, never walked inside a request — because `dispatch()` is followed immediately by `client_conn_finish()`, so this daemon has no way to defer an HTTP response; `GET /containers/{name}/stats` walked on every request, and the two remaining reactor walks are counted by `test_blocking_waits` rather than converted | Accepted |
| [0294](0294-the-dashboard-javascript-goes-through-a-parser.md) | The dashboard JavaScript goes through a real parser in `SELFTESTS` -- `test_web_syntax` links `libquickjs.a` and compiles `api.js`, `vt.js`, `app.js` and `index.html`'s inline block with `JS_EVAL_FLAG_COMPILE_ONLY`, because a textual check cannot tell the `else` that caused a live blank-dashboard outage from a legal one after a braceless `if`; node was rejected on cost (a V8 compile) and duktape on measurement (no async/await against 258 async functions) | Accepted |
| [0295](0295-a-container-in-the-directory-can-read-it.md) | An OMITTED container `dns_servers` defaults to the registered DNS servers sharing a network with it (an explicit `[]` still means none), read from each server's persisted DEFINITION rather than the live registry -- autostart order is arbitrary among the `depends_on: []` definitions the real box has, so a registry lookup would make #451's symptom intermittent instead of fixing it; a container that is itself a DNS server is excluded, its upstream being `dns_forwarders_set()` | Accepted |
| [0296](0296-the-platform-keeps-nsswitch-correct.md) | One source of truth for `/etc/nsswitch.conf` (`daemon/src/nsswitch.c`), `hosts: files dns` in both the image baseline and the `ldap_client` replacement, and the baseline written whenever its content DIFFERS rather than only when absent -- ADR-0295 gave containers resolvers that `hosts: files` meant glibc never consulted, while the containers that DID resolve worked only because the ldap variant omitted the line and a glibc default took over | Accepted |
| [0297](0297-a-query-about-a-running-job-defaults-nothing.md) | A query about a job that ALREADY EXISTS defaults nothing: `GET /pkg/build/log?name=` with no `?image=` means "whichever image is building that package", not `base` -- `pkg_chain_index_for_name()`, with `pkg_chain_index_for_target()` and `normalize_image()` untouched because defaulting is correct for a request that must choose a destination; adds that a 404 about a lookup names what the lookup found (the builds that ARE running) rather than asserting "no build in progress", and splits the transient no-build-container 404 out from it | Accepted |
| [0298](0298-a-readiness-probe-tries-every-address-the-container-has.md) | A `ready: {tcp_port: N}` probe tries EVERY address the container has -- loopback first, then each attachment, one candidate per 250 ms supervision turn -- because "listening" covers a service bound to `0.0.0.0`, to loopback only, or to one interface of a multi-network container; the addresses move onto `cixinit_hello` (per container, not per service, which is what let the daemon compute one before it had parsed the networks) and `ready_addr_be` is gone, wire version 2. Rejects the smaller fix of moving the call and keeping one address, which would have broken loopback-only binders | Accepted |
| [0299](0299-the-control-plane-root-is-written-atomically.md) | `mkbootroot` writes the control-plane root to `<out>.partial` and `rename(2)`s it onto the final name only after `mksquashfs` exits 0 (fsync file, rename, fsync dir), so that path only ever holds a whole image and a failed assembly leaves the PREVIOUS root -- the one the machine is running from -- untouched. `GET /system/assembly` additionally `stat()`s the artifact (`image_present`/`image_complete`/`image_bytes`/`image_mtime`), because ADR-0105's generation counters are in-memory and a deploy ends in a reboot, so a host that just booted what it assembled reads identically to one that never assembled anything. Derived never persisted, for `iso_recover_state()`'s reason; rejects a completion record and rejects verify-at-staging. One `squashfs_image_check()`, which the ISO builder was not using at all -- it checked `access(R_OK)`, so a fragment could be signed into installer media | Accepted |
| [0300](0300-a-build-slot-is-validated-against-the-registry-not-the-entry.md) | A chain slot whose entry claims BUILDING is reclaimed when that entry's build container is absent from the registry, and the entry is marked failed rather than left claiming to build. `chain_reap_stale()` trusted `e->state`, so ten entries stuck in BUILDING pinned all ten slots and refused every install and hostbuild on .95 with no recovery but a reboot -- the reap could not help because it trusted the one field that was wrong. main.c answers liveness via a registered predicate (pkg.c still never links `registry.h`); absence is sound because `container_exit_finalize()` calls `pkg_build_completed()` BEFORE `registry_remove()`, so a gone container has already completed. Present-but-not-running counts as live, deliberately. Rejects extending it to fetches and rejects waiting for the unestablished root cause | Accepted |
